# Testing

## 12. Testing

| File | Covers |
| `packages/filmopen_format/test/stems_test.dart` | the filename grammar: every name kind, takes back to their source and batch, rejections (a version or take number too long for an int among them), naming warnings, the reserved owner |
| `packages/filmopen_format/test/children_test.dart` | the script's list (§5.11; the format's §8.5, §10.1, §11.2, §17): `sc_1`, `fo_f4` and `sc_5_maria_v1` parsed to those, the last pinned; a dotted tag and a workspace handle; a bare tag, an entry of another type (a type the format does not know among them), a path and an entry that is not a string — a value JSON cannot hold among them, which never throws — each reported and never guessed; a type with no name (`sc_`) read as written; a second listing in one list reported on the second and later entries; 1.11's four keys meaning nothing; the three edits pure and deep-copying, leaving every other key and every unread entry alone, a file alone when the entry is not there or already there under either spelling, clamping an index, addressing equal entries by occurrence, moving a pinned entry pinned; **an entry that is not a string addressed by what a reader shows it as** — `5`, `null` and an object removed, of `5` and `"5"` the one the occurrence points at, a number moved staying a number; `nextNumberedTag` on an empty list, over gaps, among other short names, with the `f` prefix, and past the longest number a segment holds; a cue naming a `folder`, a `scene`, neither or both, with 1.11's `unit` unread; the `fo` prefix in the filename grammar and `folderKinds` |
| `packages/filmopen_format/test/scene_cast_test.dart` | a scene's `cast` (§5.13): an absent key as an empty cast, entries read as copies all the way down, anything A.5 does not describe not read; `castWithCharacters` — the entry that stays whole, a new one the name alone, the order given, typing back a file that names a character twice changing nothing, a name given more often than the file has entries adding one entry; `writeCast` removing the key for an empty cast; adding once, removing by index, an entry's own epoch set and removed with everything else kept and the entry handed in unchanged |
| `packages/filmopen_store/test/folders_index_test.dart` | a folder in the index: the versions of a `fo_` file under one group without an epoch and no unknown file; a short stem resolved as a short reference of its type (official with no viewer and for an author who has said nothing) and a pinned entry to that version alone; a folder and a scene sharing a short name; `nextSceneTag` and `nextFolderTag` over a loaded project — an empty one, one with gaps, several authors, hand-made short names, shots and cues — and a scene that is in no list counting too; and a dropped `ep_`, `se_` and `sq_` file as an unknown file — reported, never an entity, never a row — with the project's scenes beside its script; and a short name a list names — by a short stem or a pinned entry, in the project file or in a folder — spoken for although no file has it, a bare tag and another type naming none |
| `packages/filmopen_l10n/test/labels_children_test.dart` | the codes of an entry of `children` as sentences (§5.11): each of the three with a sentence of its own in English and in Spanish, the type an entry names quoted in *another type*, an entry with nothing to report having none, and every value of the enum said |
| `packages/filmopen_store/test/script_outline_test.dart` | the script as one viewer sees it (§5.12) on `foldersFixtureFiles`: the project's entries depth-first, four folders deep; a second listing in another list on the later row, its first clean; a cycle drawn, not followed, and the report where it is a second listing too; a pinned entry drawing that version while another is in use; a bare tag, another type and an entry that names nothing; each row's list, place, occurrence and depth; another author's folder drawn with what it holds; each viewer seeing their own version's list, official deciding for who has said nothing, and an undecided folder flagged with its group and not followed; *not in the script* — an empty project, a project made before folders, folders that name each other, a scene an unfiled folder holds, an entry pinning a missing version, a folder and a scene of one short name; `childRowsOf` for a version the script does not draw; `cueGroupsOf` for a folder and a scene of one short name; no project file chosen — no script and nothing beside it, a handle alone not choosing; and a cue no version of which is in use shown wherever any version says; `folderIsOrHolds` — itself, beneath it at any depth, never above or beside it, through a cycle the files already hold and out of it again, a scene, an unreadable entry and a missing folder holding nothing, a pinned entry read at the version it pins |
| `packages/filmopen_store/test/pick_test.dart` | the pick model on tiny fixtures (§6.1 step 2, §13.4): `pick` and its 1.5 spelling, `pickedAt` in the shape of `pick`; the warnings for a pick naming no file, another epoch or another entity, an unreadable time and a pointer naming another key (dropped, never official); a duplicate commentary file; the director of record — a viewer's fork of the project file selecting it but appointing nobody, in the model and in the writer — and a folder with no file of record, where a fork (once or twice, the viewer's or a colleague's) is followed back to its origin, an original decides, a loop or a missing source decides nothing, and a save that removes, changes or adds a project file's `forkedFrom` (a `null` included) is refused while a scene's is not; a misnamed project pointer leaving a library pointer standing; an offset without its colon; handles, not owners, for the project file; pick, official, own; a workspace's chain; resolution through official for official files, the project root included; and the stale predicate: a later `setAt`, equal seconds with a fraction, the next second, a lower-case `z`, offsets compared as instants, no zone, no time, the official version and the tracking value never stale, an inherited pick never stale, a pointer to a missing file superseding nothing |
| `packages/filmopen_state/test/stale_pick_tree_test.dart` | the red chain in the tree for every kind of row: the Project row, a scene with its *Versions* row and a shot row, a library entity with its epoch, a spec entity; the author's latest row above an older stale pick; a unit pinned by a full reference left unpainted; and nothing painted for a newer pick, equal seconds, no `pickedAt`, another viewer, nobody or a workspace inheriting the pick |
| `packages/filmopen_tree/test/tree_view_test.dart` | how a row is drawn: an overtaken pick's label and check in the error colour with the reason on hover, an ordinary pick untouched, a parent row red without a check |
| `packages/filmopen_tree/test/tree_drag_test.dart` | the view's half of a drag (§8.3), on four bare rows and a `TreeDragDrop` that records: a third for each edge and the middle third for itself on a row that takes all three places; halves on a row that takes only the edges; a refusal said and nothing dropped; a row that is no place asked with no place; a row over itself asking nothing, dropping nothing and saying nothing; a row that cannot be dragged staying put and still being asked; thirds at their borders (0.3, 0.4, 0.6, 0.7 of the row); the pane asked once while the pointer moves within one place and again at another; a draggable row still saying its tooltip under the mouse's hover |
| `packages/filmopen_tree/test/place_in_test.dart` | the drop's geometry alone (§8.3), by table: a third to each edge where a row takes a drop into it and beside it, the boundaries among them (1/3 and 2/3 mean *into*); halves where it takes only the edges; *into* anywhere where it takes that alone, one edge being no edge; nothing where it takes nothing or one edge alone; a pointer past either end read as the nearer end |
| `packages/filmopen_tree/test/tree_row_memo_test.dart` | what a row remembers under a drag (§8.3): a rebuild that leaves the row as it was keeps the answer, although the pane hands it a fresh `TreeDragDrop` every frame; a row handed another node asks again |
| `packages/filmopen_tree/test/row_menu_test.dart` | a row's menu built when it is opened (§8.3): a frame of the tree asking a row nothing about what it offers, the ⋯ asking once and the frames after it not again, a right click asking the same way, a menu that answers nothing opening nothing rather than reaching `showMenu`'s own assertion, and a row that is no place of the script wearing no ⋯ |
| `test/token_choice_field_test.dart` | one token out of a list drawn the same way wherever it is asked (§9.3): a `within` of another shape drawn locked, which neither a person nor an agent writes over; a token the project has nothing for said under the box as an error and offered as an entry of its own; the field's helper said in both modes, under the box and under the locked value; a key holding nothing read as *None*, with no entry of its own; choosing the entry on show writing nothing, so that a legacy `franchise` stays until the kind is really changed; and *None* over a key that is there holding nothing taking it out |
| `packages/filmopen_plugins/test/plugin_digest_test.dart` | the folder's digest (§6.11), which is what a person's *Allow* is given for: a file changed, added, removed or renamed changes the hash; a folder past either ceiling — 2,000 files or 64 MB — **is not read at all** and says which limit it met, so the registry drops its code and it can never be allowed on the strength of a hash that stands for no file; a folder that cannot be listed likewise, and never hashing empty; and a file whose bytes cannot be read named rather than passing for an empty one |
| `packages/filmopen_plugins/test/json_shape_test.dart` | the checker of the definition (§6.11): an unknown keyword refusing the schema **by name and by where it is**, a keyword of the wrong shape likewise, a `$ref` that resolves to nothing refused, an unknown keyword inside `$defs` found as the library is made, a shape that refers to itself read once and checked at any depth, an empty schema taking any value; and a wrong value by its path for `type`, `required`, `enum`, `const`, `items`, `minimum`, `maximum`, `additionalProperties` and `oneOf` — the closest shape's problems reported, a value that matches two refused, and a seed of `-1`, `4294967296`, `1.5` and `"7"` each refused where `7.0` is an integer |
| `packages/filmopen_plugins/test/plugin_api_test.dart` | the definition itself (§6.11), read from the repository: its two numbers, `status` required of every plug-in and answering `Status`, every function carrying what it receives, what it answers, its state, its revision and who may call it, every interface saying what a manifest adds, `ctx` with every member's kind and state, `render`, `complete`, `transform` and `verifyKey` reachable by no plug-in, and every example valid against its own arguments; and a file this reader cannot hold a plug-in to refused whole — an unknown keyword, an example that is not valid, a function without its state, its answer or who may call it, a `$ref` to nothing, a field nobody reads, a `since` after the revision, a state the file does not define, text that is not JSON |
| `packages/filmopen_plugin_host/test/contract_test.dart` | the host against the definition (§6.11): every door of `doorNames` a member of `ctx` and every member the app answers a door, `ctx.upload` planned and no door, **what a running call is given** equal to what the definition says `ctx` carries with `ctx.api` the file's two numbers, the host calling the functions the definition says the app or a person calls and no other, and **the arguments the host builds** for a render (its entry, its seed and its source), a complete, an estimate, a key check, a `transform` and the three the developer page calls each valid against that function's own `args` (the complete's through the app's call; `ctx.ai.complete`'s own road is `plugin_calls_test.dart`'s) |
| `packages/filmopen_plugin_host/test/reference_test.dart` | the generated documents against the definition (§6.11): both naming the script that writes them and saying they are not edited by hand; the types holding every member of `ctx` with nothing planned inside `interface Context` and every planned member in its own namespace; every function of the definition in `interface Plugin` or, planned, in `PlannedPlugin` and not in both; `REFERENCE.md` listing every function with whether it is required and its state, every member of `ctx`, every state and the file's two numbers, and what each interface adds to a manifest; **the guide stating no shape the reference does not** — every shape `README.md` writes outside its examples, a function's arguments among them, being one of the definition's objects, whole unless it says `…`, each field optional exactly where the definition says, and `hooks` and `provides` named only where it says api 1 no longer reads them; and the repository's own template carrying `"kind": "tool"`, two code files, an offer and a call on itself, its two locales holding the same keys |
| `packages/filmopen_plugin_host/test/status_test.dart` | `status`, who may call what, and a plug-in that needs a newer FilmOpen (§6.11): each of the four states answered with the plug-in's own words, a `status` called with `ctx` alone, one whose code has none told so **with its other functions still running**, one that throws a failed call with its own message, an answer the contract does not hold `badResult` **with its path** — `state` for a state the file does not define and for none at all — a plug-in that is off running nothing and one that is not installed said so; a platform plug-in answering `run`, `request` and `status` to a caller that declares the call, every plug-in answering `status` whatever it implements, and `render`, `complete`, `transform` and `verifyKey` refused `notOffered` **even when the manifest tries to offer them**, as are the names the language gives every object; a call the caller did not declare `no-grant`; a `minRevision` past this build's listed, said and unable to run, and one at this build's running; and `capabilities.upload` known, said on the page and answered `notSupported` in those words; a check refused `ctx.ai.complete` with the reason `status`, **and a plug-in the check calls refused it too**, while the same chain started by a person's action is not; and, since step 7, a `status` an action reaches through `ctx.call` a check too, refused `ctx.ai.complete`, and a platform's `run` refused inside a check and reached outside it |
| `packages/filmopen_plugin_host/test/render_choice_test.dart` | the choice by model and platform, the prepared request and the answer by state (§6.11), against a recording `MockClient`: two plug-ins on fal each naming its own model reached for its own render and estimate whatever the preference says; a model only a generic plug-in serves reaching it, and one naming the model winning over the generic; two naming it told apart by the preference and, without one, `noProvider`/`choose` with the app's sentence; no model, no entry and nothing that serves it each said; a request naming `comfyui` never reaching a plug-in on fal, and none there said with the platform; the local one taken before one that bills, two that bill `choose`; the local one failing with nothing sent to fal and no key spent; an estimate told `operation`, and one without `badArgs`; an input the entry does not list, a required one missing and a required prompt empty refused by name, and inputs with no film open `noFilm`; an answer without `state` `badResult` at `state`, `failed` with the plug-in's code and words, `pending` `notSupported` in words, a preview not a take; the item recording the app's model, platform, prompt, params and inputs whatever the answer says, `stack` and `seedApplied: false` from it and then no seed, and a seed of 0 applied; an error page sent as `image/png` refused `failed`/`mime` with nothing kept; and the prompt in the batch, never in the log or the usage records; **this computer's plug-in before the person's preference** (a preferred fal plug-in not taking Z-Image from a local one naming it, a request naming fal followed, two local ones told apart by the preference, a preference cleared asking again); a `stack` over 200 characters left out; and `ctx.ai.complete` with no model named answered by the only text plug-in, `choose` with its own sentence where two are on and nothing says which, and the preferred one where one is; and, since step 7, a required list that names nothing (`reference_images: []`) refused as missing |
| `packages/filmopen_plugin_host/test/key_paths_test.dart` | a key goes only where its platform file allows (§6.11), against a `MockClient` that **records every request that reached it**, with made-up keys in a store in memory: fal's key to `https://api.fal.ai/v1/keys` refused `path` with nothing sent — by `url` from the plug-in that owns it, from one granted it, and through a platform plug-in's `request` called by another — while `/v1/models/pricing` is reached with the key; a `path` joined to `queue.fal.run`, which takes any, and one naming another host (`//…`, a scheme) `badArgs`/`path`; OpenRouter's `/api/v1/key` reached and `/api/v1/keys` refused by `path` and by `url`; twelve spellings of a way out (`..`, `./..`, `%2e%2e`, `%2E.`, `%2f`, `%5c`, `//`, an empty segment, a backslash, a control character, `..;x=1`, a `%` that is no escape) and two by `path` each `bad-path` with nothing sent; a redirect to a path outside the file refused `redirect` and never sent; the app's OpenAI key, granted, reaching `/v1/responses` and `/v1/models` and refused `/v1/organization/admin_api_keys`; a plug-in's own platform file without `paths` taking any path of its hosts; and the seam carrying a called plug-in's key platforms by name — its own keys, a key it asks to use and, through its own calls, theirs, a cycle walked once and the caller's own keys left to its first line — none for one that may spend none, a grant's platform whether or not its owner is on, the own slots in words, and a function of the contract called by name in the app's words; and, since step 7, **a key sent by its own platform's file** — another plug-in's file for the same id, read first, allowed or never allowed, deciding nothing — and a plug-in's `Host` and `Content-Length` dropped while its own header goes; and a plug-in that is off naming the platform it brings on its page, from its own file |
| `packages/filmopen_plugins_ui/test/consent_spends_test.dart` | consent says what a call may spend (§9.4, 5-2's), in English and in Spanish: the app's line, indented, under a call on a plug-in owning keys, one per platform, its words further in than the call's, and under each key the plug-in asks to use — the app's own and another plug-in's — and nothing under a call on a plug-in owning none; the keys named in words and never by id; the longest line wrapping at 700 and 375 px in Spanish and dark under its own words and not under its mark; and the page saying the same under the call and under each grant's checkbox |
| `packages/filmopen_plugins_ui/test/plugin_check_test.dart` | *Check* on a plug-in's page (§6.11): the button on every page, running **only on the gesture** and never as the page draws; each of the four states in the app's words with the plug-in's own message beside it, in English and in Spanish; a plug-in whose code says nothing, and a check that failed showing `badResult` with its path; *Check* disabled with the reason while the plug-in is off; what it implements listed with its platform and its models; the upload door said to be not in this FilmOpen yet; a plug-in that needs a newer FilmOpen naming the revision at 700 px in Spanish, and one that cannot run for another reason not told so, nor that this FilmOpen is the cause; an answer forgotten when the language changes; and *About* saying `archiveTooLarge` and `badCodeFile` in their sentences, in English and in Spanish, never by the code, while a code with no sentence is still named with its field |
| `test/plugin_api_asset_test.dart` | the definition travelling with the app (§6.11): the bundled asset equal to the repository's own file and parsing as the contract, listed beside the stock plug-ins, and no plug-in folder taken from a file at the top of the bundle |
| `packages/filmopen_plugin_host/test/dev_run_test.dart` | the developer page's side of the host (§6.11): the development folder's sub-folders read as plug-ins **before** the library's and before a stock plug-in of the same tag, never trusted — so a working copy asks in ordinary use where the installed one asked nothing — a sub-folder that is not a plug-in passed over in silence, the preference winning over the environment and the environment serving where it is empty, naming and clearing remembered, and a folder that is not there listing nothing; a run of a plug-in that was never allowed, which **still asks in ordinary use afterwards** with nothing stored, a key nobody granted sent and recorded under the hook `dev.<function>`, a code file changed between two calls giving the second call's result with no reload, a failure carrying its code, its message and its place, and a plug-in that is not installed answering `noProvider`; the log listing the request as sent first, in the page's list and in `plugins-dev.jsonl` alike, then every `ctx.log` line and every door in order **with no credential**, each line reaching the caller **as it happens** and not when the call ends, a refusal as its own line with its reason and no address, data cut at 64 KB with a mark and a message at 200 characters; `last-run.json` holding the call, the outcome and the log under a megabyte at the path the page shows, and `devClose` forgetting it; and what the page is told — the functions being the actions, the hooks and the offers and **never `verifyKey`**, the problems by file, the two paths, and nothing for a plug-in that is not installed; and **the repository's own template**, copied into a development folder as its README tells a person to, reading with no problem and running its functions from the page — `status` as *Check* runs it, the offer, the action that reaches it through `ctx.call`, and the `transform` on a scene, shown and not written; and, §3.6, **the development folder read one level deep** — two plug-ins beside `docs/`, `research/`, `.git`, `node_modules` and a plug-in folder that cannot be read, both listed and run, a reader counting what it was asked showing the folder asked once, only the tops of folders named like a tag, a plug-in's whole files only for a plug-in and nothing of `.git` or `node_modules`, the unreadable one named with its path and logged once, and one that cannot be looked into passed over and said in the log once with its path; the function list `status` first, then what it implements, its actions and its offers, with the definition's examples; a render as FilmOpen calls it prepared — its entry, its seed — shown first as the run's `request` line, carried on the run, and saved as a take whose item is the app's own, and as typed sent as the box with nothing saved; `last-run.json`'s `mode` and its request as sent, an action as FilmOpen calls it with `ctx` alone; an answer of the wrong shape `badResult` at its path either way; and `status` unable to reach `ctx.ai.complete`, as *Check*, whichever way it is called, its `message` the plug-in's own words and never looked up as a string key or written to the app's log; and, as FilmOpen calls it, a model the catalogue lacks, one the plug-in does not serve and a completion's `params` that are no object refused before the plug-in runs, `last-run.json` then holding no request, while as typed the same request runs |
| `packages/filmopen_plugin_host/test/bundle_tags_test.dart` | the app’s own bundle listed **once** (§6.11, §14.5): the tags a film may not take are read from the bundle, and were being read on every project reload — every refresh, so as often as a person saves a field; the same source is listed once however often a project reloads, another source is listed on its own account (the slot is matched by identity, so it can never answer for a source it was not read from), and a listing that **failed** is asked again rather than remembered as empty, which would fail open and let a film’s own folder take a stock tag and the keys bound to it |
| `packages/filmopen_plugin_dev/test/plugin_dev_page_test.dart` | the developer page itself (§9.4a), over a fake runtime: the standing sentence; a plug-in chosen filling the function pull-down, a function filling the argument box with its shape as JSON; text that is not JSON said inline with *Call* disabled and enabled again when it is corrected; *Call* passing the function and the parsed arguments and the result line showing a `PluginCallDone`'s words; a failure showing its code, its reason, its message and its `file:line:column`; the log's rows in order each with its own identifier, **growing while the call still runs** and a line that arrives after it dropped, and *Clear*; *Stop* (`plugin-dev.cancel`) only while a call runs, stopping the control; a stock copy's sentence and a plug-in's problems by file; `set_field plugin-dev plugin\|function\|args` typing as a person does and refusing what is not there; two entries of one name drawn as one item; and the page **measured** at 375, 700 and 1200 px in dark and in Spanish — every identifier's rect inside the dialog and none crushed, taken **while a call runs**, so *Call* and *Stop* are measured in their `Wrap`; and, §3.6, the two ways of calling — *As FilmOpen calls it* chosen by default and passed to the host, *Exactly what I typed* a tap away, each with its line, `set_field plugin-dev mode` taking `app` and `typed` and refusing anything else — the line that the box is not sent for `status` and an action as FilmOpen calls them, a plug-in of the development folder that cannot be read named with its path, a failure's path in the result line, `status` named *every plug-in's* and never by its code, *Save what it makes to* there only as FilmOpen calls it, and the page whole at 375, 700 and 1200 px in dark and Spanish and in light and English, the mode chips and the folder's problem measured inside the dialog |
| `test/plugin_dev_entries_test.dart` | where the page is reached from (§9.4a): a platform that runs no plug-in shows neither *Plug-in developer page…* nor *Plug-ins in development*, **and one that does shows both**; the development folder typed and entered, and emptied; the entity the page is opened on — the version a tree row names, the version in use at an epoch row, and none on an overview or on an entity as a whole; and the seam's own doors answering for a page that is not there rather than throwing |
| `packages/filmopen_plugin_host/test/render_inputs_test.dart` | what a render is told, and the script a plug-in reads (§6.11): a first frame and two reference images arriving as descriptors with what the batch recorded and **no bytes**, one reference where a list is expected read as a list of one, nothing and an empty object `{}` and an empty list refused `badArgs`/`inputs`; a name the catalogue does not know refused `badArgs`/`inputs` **with the name**, with no film open too, and a known one with no film open `noFilm`; a reference that is no media, an empty one, a number and null refused with the plug-in never running; every render with no seed given one in the catalogue's range, six renders not all the same; **a caller's seed — 7, 0, 7.0, or in `params` — being what `render` receives and what the take's item records**, the top of the range and 0 among them, and `-1`, `4294967296`, `1.5`, `"7"`, `true` and two seeds that disagree each `badArgs`/`seed` with the plug-in never running; `source` naming the entity with `ctx.project.get` answering its document, and no `source` for a render for nothing; `ctx.project.script` answering the folders and scenes in order with each row's type, tag, stem, name, kind, depth and children, an entry that names nothing carrying its problem and no stem, and a film with no script answering nothing; and `kind` and an interface's `models` — an entry the catalogue has listed, one it lacks reported with the plug-in still running, a kind the vocabulary lacks kept as written, and a plug-in that says neither having neither; and **what the batch records of a render's media** — `inputsByName` under the catalogue's names, with what the import measured, beside §12.2's flat `inputs[]`, written **per name**: a name holding media is recorded and a name given an empty array is not, so the key itself is absent when no name holds anything, and none of its maps shared with the request it was prepared from |
| `packages/filmopen_plugin_host/test/plugin_calls_test.dart` | plug-ins that call plug-ins (§6.11), with the real engine: a granted call running as its owner — the owner's tag, settings and storage, never the caller's — `ctx.caller` naming who asked by tag and stem, and **null** where the person started the call, and only the JSON the function answers coming back; refused — a call the manifest does not declare (`no-grant`, declared by function and not by plug-in), a missing owner (`notInstalled`), one turned off (`notEnabled`), one on and unable to run, its code gone (`cannotRun`, never told to turn on what is on) and a function that is not offered (`notOffered`, an action among them), and a name that is not `<tag>:<name>` (`badArgs`); three levels below the person's call running and a fourth refused `depth`, a function reaching itself through another refused `cycle`, and a plug-in calling its own offer, which is the template's example; six calls at once from one frame reaching the owner one at a time, a nested call making a call of its own **not** deadlocking, a caller that answers without waiting ending its whole chain with no request after it, and a key the called plug-in sends recorded against the plug-in the person ran; consent's lines grouped by the plug-in called in that plug-in's words, a plug-in that is not installed named and said to be missing, what a plug-in offers listed in its own words, and **nothing about calls stored** after *Allow*; and an output on another plug-in's server downloaded where a call on that tag is declared and `no-grant` where it is not; and, since step 7, `ctx.ai.complete` given no conversation, a string or a message without its content refused `badArgs`/`messages` with nothing sent, and a completion that waited its turn behind another refused `noPlugin` when its plug-in was turned off meanwhile, as a `ctx.call` queued the same way is refused `notEnabled`, the key never spent a second time |
| `packages/filmopen_plugins/test/plugin_manifest_test.dart` (the `implements` group) | `implements`, `minRevision` and the capabilities (§6.11): an interface read with its platform and the entries it speaks for, and the functions it brings; one declared without what it must add reported and left out with the rest working; a declared value of the wrong shape reported by its path; an entry id the catalogue lacks kept and one that is no id reported; `minRevision` a whole number from 1; `capabilities.upload` known and an unknown capability still reported; what a plug-in answers to another plug-in read from the contract (`platform`'s `run` and `request`, every plug-in's `status`) and never a render; an offer's own `args` and `returns` read, one this checker cannot read reported with the offer still readable; **the door's own rule**, `plugInsMayCall`, refusing a contract name through an offer on a manifest built by hand to carry one; an interface bringing only the functions this build can call; and **the plug-ins repository's own two manifests** — `fo-cui` and `fo-cui-zimage`, written to the contract — reading with nothing reported at all, their `x` kept and their `calls` in order |
| `packages/filmopen_plugins/test/plugin_manifest_test.dart` (the `offers`/`calls` group) | `offers` and `calls` (§6.11): an offer's name, label, help and example; one without a label reported and left out; a name that is not a plain name and an entry that is not an object reported; an unknown field reported with the offer still read; calls kept in order with a repeat counted once, the plug-in's own tag allowed, and eight malformed ids reported and left out; `CallId` reading, writing and comparing; and a manifest that declares neither reading as it always did |
| `packages/filmopen_l10n/test/labels_plugins_test.dart` (part) | **a render's own refusals and one outcome's one sentence** (§6.11, §10): the eight codes `prepareRender` and `RenderInputs` raise, each with its sentence in English and Spanish and none of them showing its own code or a field name; `pluginOutcomeText` over **every code `PluginCallFailed` can carry** × seven reasons × both languages, each answering a sentence and only `cancelled` counting as no failure; a call that finished saying *Done.* or what it wrote; a plug-in's own words travelling; and the two the dialog alone used — a changed file and a missing handle — said here too; and **`ctx.upload`'s own sentence in both languages**, which nothing pinned until round 1 |
| `test/plugin_hook_test.dart` (part) | **a render's refusals on a screen, in the longer language** (§6.11, §10): three of them laid out in the hook dialog at **700 px in Spanish** — the media it was given, a model that needs an input it has none of, and a prompt it has none of — each found as the sentence the ARB holds, and each **measured**: no wider than the dialog, and as tall as the sentence needs at that width, so nothing is clipped (a clean console is no evidence that a narrow layout fitted). **The dialog is a stand-in for the typography, not the road**: a render's refusal reaches it from nowhere today, since `prepareRender` is reached only through a platform hook, and the surface that shows one now is the developer page's result line — a render dialog is the one a person will meet it on. A step whose product is words owes a screen, and the longest of these carries a catalogue input name |
| `packages/filmopen_plugin_host/test/one_pipeline_test.dart` | **one pipeline** (§6.11), against `MockClient` with the real engine: for the same box, the request the developer page sends *as FilmOpen calls it* and the one `call` sends are **equal key for key**, the seed aside where neither was given one, and a seed that was given is kept on both roads; the page's own record of what it sent is that request; a completion's `messages` are held to the definition on the page as in the app; **a page render on a key is recorded with the model's entry and the kind `image`**, under `dev.render`, with `started` and `succeeded`, while the app records the same call under `render`; a render the plug-in says `failed` is recorded **failed**, not succeeded; `verifyKey` *as FilmOpen calls it* is `notFound` with nothing spent and nothing sent; a model the plug-in does not serve, and one the catalogue lacks, are refused before anything runs, as the app refuses them; and the first line of a page render is the **request**, before any door. **The page runs the plug-in whose page it is**: with two plug-ins serving one model the page answers the one it was opened on, while the app, asked the same thing, refuses `choose`; and a platform hook from the page sees the `ctx.selection` the app gives it, which is none. Four of the twelve fail against step 3's code, and one more against a merge that let the page's choice be the app's |
| `packages/filmopen_plugin_host/test/run_binding_test.dart` | **a run is of the film it started in, and of no other** (§6.11), against `MockClient` with the real engine: each test holds a render or a transform at a door, moves the running app, and lets it finish. The take and its batch land in the film the render began in, by the person who began it, while the film opened meanwhile holds nothing new and `ctx.project` answered the first film; the same where the second film is a **copy** with the same stems and the same text; the viewer alone changed and the author does not; the film **closed** and the takes are still written with nothing re-read and the outcome `done`, while the film still open is re-read once; the plug-in's `server` setting changed mid-render and the output still comes from the address it started with; a render with **no film** answers its outputs, ingests nothing and saw no entities; a `transform`'s versions and batch in the film it began in, with a source changed **in that film** still `stale` and nothing written over it; and **being bound is not being allowed** — the owner of the server turned off while the render waits is `no-grant` with nothing fetched and nothing written, while the same render is `done` where it stays on; **a handle typed while a run waits reaches neither what it reads nor what it writes** — `ctx.settings` and a batch's recorded settings are the project file of the person who began it, and a take's author is that person; a write into a film that is not on screen re-reads nothing; and a call that waited for one of the app's five slots, all five held by other plug-ins, is given the `ctx.settings` and the `ctx.l10n` of the moment it was asked for, not of the film and language it finds when a slot frees. The fixtures report a folder, each its own, so a binding test is not also a test of the refusal. |
| `packages/filmopen_plugin_host/test/server_grant_test.dart` | **who may fetch from whose server** (§6.11), against `MockClient` with the real engine, on manifests shaped like `fo-cui`'s and `fo-cui-zimage`'s: the contract's own `run` authorizes the fetch and so does `request`; each of five custom offers still does; **`status` alone is refused `no-grant`** with nothing sent and nothing written, while `status` beside `run` is allowed; no declared call, an empty list, and a call on a function the owner does not have are each refused; the owner turned off and the owner unable to run are each refused; a plug-in that is not installed at all is refused; the answering plug-in's own `server` works with no call to declare; and `fo-cui-zimage`'s four calls pass both as written and **with `files` and `nodes` taken away**, which is what X1 was about. a call declared on a second platform plug-in does not authorize a fetch from this one; `run` authorizes only where the owner **implements** `platform`; a function of `platform` that is still `planned` does not authorize it, which is the tripwire for the day `collect` is built; and a refusal says so in the log with the tag and the cause and neither the address nor the path. Three of the fourteen fail against the old gate |
| `packages/filmopen_plugin_host/test/server_door_test.dart` | `ctx.http({ server, path })` and a render's output on a server (§6.11), against `MockClient` with the real engine — a held value that does not parse refused `no-server` at the door and in an output alike, with nothing sent, the plug-in's own tag allowed to qualify the name in both places, the download of a server's output **told not to follow a redirect** (asserted on the request, since a handler-backed client never follows one) while an https output is fetched as it always was, and an address whose bytes cannot be read refused rather than thrown: the stored address used with its base path joined, the manifest's default where nothing is stored, no credential in a plug-in that owns a key and no usage record, an `Authorization` header the plug-in set itself sent, every method and a body and a query as written; refused — a name that is no `server` setting, `key` or `url` beside it, no `path` and five paths `resolve` refuses, nothing stored and a stored value that does not parse, and a redirect to another port, host or scheme while one within the address is followed; a `server` value in a project file never read; an `http` `url` still `insecure` and a `capabilities.network` host still `https`; and the outputs — `{ server, path }` downloaded without credentials with its thumbnail, the qualified form of the plug-in's own tag, another tag `no-grant`, both forms in one output `shape`, and a name that is no setting, no address stored and a path that cannot resolve |
| `packages/filmopen_plugins_ui/test/server_box_test.dart` | the address box and consent's line (§9.4): an address saved normalised once the typing settles, text that is no address said inline and saving nothing with a later address still saving, emptying the box removing the value, a public `http` address warned in the tertiary colour **and saved all the same**, a private address and a host name and `https` not warned, the plug-in's own help winning over the app's, an agent's `set_field` following a person's rule and refusing what is not an address, the box whole in dark and Spanish at 375, 700 and 1200 px, and consent naming the setting in the plug-in's words with and without its default |
| `packages/filmopen_format/test/project_key_test.dart` | **which film this is** (§5.15): two folders holding the same film have two keys and one folder read twice has one; one folder spelled five ways — a trailing separator, forward slashes, mixed slashes, doubled slashes, leading and trailing spaces — is one key, while **case is kept** whatever the path looks like, a false join costing more than a false split; a root path that names no folder is a session of its own; a Windows share's root keeps its two leading separators; a root is itself (`/`, `C:\`, `E:`); two projects in memory are two films however alike their files, and one keeps its key through a write and every re-read; a session copy over a base is a film of its own and not the base it reads through, which is the shape the bundled sample's is (the real one is `filmopen_platform`'s, which the format may not reach); and a `FilmProject` is keyed by the source it was read from, every load of one film answering one key |
| `packages/filmopen_format/test/server_address_test.dart` | the address of a server a user runs or chooses (§5.14): this computer, a name, a base path and IPv6 read; the scheme and host lower-cased, a trailing slash and the scheme's own port dropped, the text trimmed; a dot segment already removed by `Uri.parse` and unable to climb above the root; refused — another scheme, no scheme, user info, a query, a fragment, an empty host, a port of 0 or 70000, a `//` path, nothing at all; `isThisComputer` and `isPrivate` over every range of §5.14, a host name private and a public address neither; `resolve` joining with and without a leading slash, a base path first, a query kept and a fragment dropped, and null for an address, a `//` start, `.`, `..` and an empty segment; `sameOrigin` over scheme, host and port with a scheme's own port counting as none; two spellings of one server being one value; **a percent-escape escaped exactly once** — a base path of `/My%20Comfy` sent as itself and not as `/My%2520Comfy`, a route's escapes read back, a literal space or accent escaped once, a segment mixing the two surviving whole, a character above the basic plane among them, `%2F` kept inside one segment, `%2e%2e` refused as `..` is, and an escape that is not two hexadecimal digits refused — a sign or a space among them, so `100% final.png` is a refusal and never a different route — while `Uri.parse` rewrites `%zz` and `%2` in a **base path** to a literal per cent sign; an escaped dot segment (`..%2F..`) refused while `a%2Fb` passes, a control character refused so a server that truncates at NUL cannot be fooled, and an escape whose bytes are not UTF-8 answering null rather than throwing, since a person types into the box and a manifest ships a default; an IPv4-mapped IPv6 address read as the IPv4 address it names and a rooting dot not part of a name |
| `packages/filmopen_plugins/test/plugin_manifest_test.dart` (the `server` group) | the `server` setting (§6.11): machine-scoped with no scope and with `scope: machine`; `scope: project` reported and machine-scoped anyway; under `entitySettings` reported and left out; a default that is this computer kept and normalised, one elsewhere reported (`badHost`) and dropped, one that is no address `wrongType`; `accepts` taking what `ServerAddress` reads and nothing else; the label and help locale keys |
| `packages/filmopen_js/test/files_test.dart` | a call of several files and where an error is: three files loaded in order into one scope with a `const`, a `let` and a `class` of an earlier file used by a later one, a later file replacing what an earlier declared, only the last file's value the plug-in, one file through the one-file constructor unchanged, the sandbox the same in every file, a door reached from a function an earlier file declared; a `throw` on a line of the second file giving that file, line and column, from an `async` function too, a syntax error giving its own file and line, no `<prelude>` frame ever counting, a value thrown that is no error having no place, a script's own `stack` believed only where it names one of the call's own files and bounded, a limit keeping no place, and a door's refusal naming none |
| `packages/filmopen_plugins/test/plugin_manifest_test.dart` (the `entry` group) | `entry` as a file name or a list: absent it is the stem's own name; one string is one file; a list is kept in order and a folder inside the plug-in allowed; `..`, an absolute path, a drive letter, a backslash, an empty segment, `./`, a file that is not `.js`, the same file twice, a thirty-third file, an empty list and an item that is not text are each reported by the path they concern and the plug-in does not run |
| `packages/filmopen_plugin_host/test/plugin_files_test.dart` | the host's side: three files running as one scope through an action, a one-file plug-in unchanged, a file the manifest names that is not in the folder listed and refused `noCode`, a bad `entry` leaving the plug-in listed with its path named and unable to run, and a file of the folder that cannot be read reported and hashed as no readable file can be, more than 2 MB of code refused with the limit named, an `app:` string named in any file resolved; a throw and a syntax error each giving their file and line through `PluginCallFailed.file/line/column` and `at`; the consent hash changing for a byte of `assets/`, of a platform file, of a locale file or of the code and for a file added or removed, a picture hashed by its bytes, and a plug-in a person allowed asking again once any file changes; the stock record's trust check over the same files — every file matching is trusted, a changed asset is not, a file beside the record is not, and a file beside the record that cannot be read is not either |
| `packages/filmopen_state/test/project_test.dart` | loading the sample: counts, warnings, resolution rules, the tree's shape — the script drilled down through its folders, every row name first (a folder its name, its kind in words and its stem, a scene, a shot and a cue their name and their short name), the episode's music cue under its folder before what the folder holds, nothing under *Not in the script* — and a pick an official version overtook painting every row up to its entity (§13.2) |
| `packages/filmopen_state/test/fixture_test.dart` | spec behaviours on tiny in-memory projects: a file official names reading the official world while the same reference read from its author's own file reads theirs (§6.1 step 2), ambiguous resolve, `default`-only fallback, project selection (ask, invalid pointer, sole author handle — a workspace's project fork counts as another author), tolerance of wrong types, leaf scenes and unresolved rows in the tree, cycle detection; and, the Script branch on folders — an entry that names nothing shown as written, an ambiguous scene a warning row from which its versions are still reached, a folder that lists itself through another reported and not recursed, and on `foldersFixtureFiles` a folder row's name, kind and stem with a token the vocabulary lacks as written, folders and scenes mixed in one list with a pinned entry drawing that version, each of the five reports as a flagged row with its sentence, a folder's cue under the folder and a scene's under the scene, *Not in the script* flagged below the script with a folder's contents beneath it, and two equal entries as two rows with two ids; a kind the vocabulary lacks as written with its capitals and the vocabulary's own in lower case; the Script row counting items; a folder's *Cues* tooltip, a scene's two, and an explicit list naming one cue twice as two ids; no *Not in the script* while no project file is chosen |
| `packages/filmopen_state/test/children_controller_test.dart` | adding to and removing from the script in the controller (§7): a folder on the script and a scene in a folder — the file, the pick, then the entry at the end of that parent's list and of no other; another author's parent, no handle, a taken short name, a `children` that is not a list and a parent a save would refuse each refused with nothing written; a parent changed on disk meanwhile — the new file made, beside the script, the answer saying why, the changed file not overwritten; an open draft that cannot be written refusing the add; **a form open on the parent** — what was typed after the settle and the new child written together through its draft, the next keystroke saved against the text that write left, a draft on another file left alone, a refusal of the draft's write as the add's answer; removing — nothing deleted, the scene then beside the script, the occurrence choosing among equal entries, another author's parent and an entry that is not there changing nothing; the one walk — `scriptOutlineProvider` answering the same object to a second reader and to the tree, following a write, and a `TreeBuilder` handed a walk drawing that walk and making none of its own; `moveChild` — within one parent one write to the place asked, to the end without an index, and nothing written where it already is; between two parents in the destination and out of the origin, a pinned entry still pinned and the pin still choosing its version, a place in the destination; the destination written first, a second write that fails answered with the entry listed twice and reported; from beside the script one write; a folder into itself or beneath itself refused with nothing written, and beside it allowed; either parent being somebody else's refused before anything is written; a destination that names it already taking no second listing and answering `alreadyListed`; **the origin written as it was checked** — a source that rewrites the origin while the destination is being written, the move's second write then refused as stale, the outside change whole and the entry listed twice; an entry that is not a string removed as the row that was pointed at, its neighbour of the same text staying; **a write whose own re-read is overtaken** — a source that starts a second, slower re-read from inside the first one's listing — still read as landed, the re-read made again; an origin that no longer lists the entry making the move the add alone; a destination that is gone refused as `ChildrenChangedMeanwhileException` with nothing written, and the three refusals' sentences coming from `WriteProblemText`; **a parent the draft would make unsaveable refuses the add before the file is made** — a name emptied after the last settle, the dry run made against what the form would write, nothing created and the draft not written either |
| `packages/filmopen_store/test/directory_source_test.dart` | on a real temp folder: the path sandbox, skipped folders and `.tmp` siblings, and the write path — the sandbox again, a changed file and an existing file refused, no temporary left behind, a delete that honours what the caller read; and the binary half (§6): a picture written and read back byte for byte with its length, the same sandbox for bytes, an existing media file refused under `create` with no temporary left behind, and a 500 MiB stream copied through without the process growing by a tenth of it; the label being the folder's name whichever separator the path was written with — two of the six spellings biting on either platform — and the project key of §5.15 on the real folder source — one folder one film however often it is opened, a trailing separator the same folder, another folder another film |
| `packages/filmopen_format/test/project_source_test.dart` | binary I/O in the sources that need no disk: a memory source writing, reading, listing and locating bytes as `BytesMedia`, text read as bytes, `create` refused whichever kind is already there, a delete taking the bytes; and `OverlayProjectSource` — reading through, keeping every write and delete to itself, refusing `create` for what the folder behind it holds, refusing a stale write against what it shows, and wrapping a related folder in an overlay of its own |
| `packages/filmopen_format/test/media_names_test.dart` | what an upload is called and where its thumbnail lives: the kind from the extension (upper case, a dotfile, a name without one), the prefix naming the purpose on every library type (a character's audio `vo`, an outfit's and a location's `am`), the names of one action numbered per purpose and parsing back through the grammar as takes of that source with no naming warning, the eight-hex id of the second, a second later, the next free one when it is taken, a local instant and its UTC twin; the thumbnail's path for a stem, a file and a nested path, and none for a URL; the reserved folders, `_inbox/` excepted; **and the extension a type would be given (`mediaExtensionForMime`, §6.13): the subtype where it is one of that kind's, never crossing from one kind to another — `audio/mp4` is `m4a` and never `mp4`, `audio/webm` and `video/ogg` nothing at all — every answer it gives an extension this application accepts, and nothing for what is not a type** |
| `packages/filmopen_format/test/media_slot_test.dart` | **where references go, said by the caller** (§5.10): where each of the four slots is, in a file with no `refs`, a plain array and groups, with the images of a plain array being that array; two slots naming one place being equal; a named group made where the file has none; at the back by default and at the front when asked; the plan's own worked example — `[A,B]` into `[X,Y]` first, then `[C]` — and a replay of either changing nothing, not even the order; one already there neither repeated nor moved while the rest still go in; a selection naming one twice putting it in once; an empty reference being none; a voice and a dotted path; the document handed in never edited; a clip turning a plain array into groups with the pictures kept in order **and the images group of a plain array joining that array rather than replacing it**, through the wrapper too; a place of another shape left alone with the answer not naming them, `referencesIn` reading nothing from it and what it does read being in file order without repeats or empties, and a write with nothing to write making no empty place; `preview` proposed only where the file has none, only for a reference the place names, never over the author's, and never where the caller names none; and `slotFor` being the kind-to-place rule `withReferenceAdded` always followed, an outfit having no voice |
| `packages/filmopen_format/test/media_refs_test.dart` | §9.0's rule for a hand-supplied reference: a picture starting or joining the plain array, joining or making `images` when `refs` is grouped, and becoming `preview` only when the file has none; a clip turning a plain array into groups and joining `motion`; a sound in a character's `voice.refs` and in `sound` on a location, an outfit and a prop; nothing repeated and nothing removed; the document handed in never edited; a value of another shape left alone; and what a file names, in order without repeats |
| `packages/filmopen_jobs/test/job_record_test.dart` | what the journal keeps of a job (§6.12): a round trip keeping every field and the instant in UTC; a key a newer build wrote kept and written back, and never standing in for one this build owns; a phase this build does not know read as *needs attention*; text that is not a record — and a payload of another shape — answering null rather than throwing, while no payload at all is still a job; a place taken off a job, and a state argument that is not text left out rather than printed as the word `null`; and the payload rule — **sixty-nine credential names refused and seventy-five ordinary ones allowed**, both counts pinned by the test itself so that a name dropped from a walk shows here; `sessionId`, `apiKey2`, `tokenHeader`, `connectionString` and `openaiApiKey` among the first, `maxTokens`, `keyFrames`, `keySignature`, `tokenCount`, `keyArt`, `hmac`, `salt` and the format's own vocabulary among the second, each singly and all together; **every name of the list, every tail, every tail qualifier and every value opening** walked whole so none can be dropped in silence, each tail also proved to fire only at the end of a name and only where the value holds text; an address refused **anywhere in a value**; a value no JSON can hold, and a payload that holds itself, refused as `unwritable` before the write would throw; and the six names the tail rule costs (`projectKey`, `placeKey`, `idempotencyKey`, `cacheKey`, `sortKey`, `groupKey`) named, with each allowed again where its value is a number |
| `packages/filmopen_jobs/test/scheduler_test.dart` | the scheduler (§6.12), on a clock and a runner the test finishes by hand: a lane of capacity two running two of five and starting the third as one ends, **in `seq` order whatever order they end in**; capacity as data, raised and set to zero; a job of another lane not held by a full one; a lane on hold running nothing while its jobs carry the hold's code, its arguments and **the action that lifts it, which the runner does not offer** — pressing it releasing the lane and reaching the runner like any other — while a job that has ended and one whose bytes are still moving are not waiting on the hold; a report after the job has answered changing nothing; two views of a job that did not change being equal; a waiting job neither running nor blocking the one behind it; a retry waiting until its `notBefore` and then running, **after a restart too**, and one that failed for good never running again; a wake taking up the jobs it names and leaving the rest; a stop reaching a queued job as a cancellation and a running one as an intent, with what the job comes to being what the runner answers — a pause leaving it to be taken up again, a stop not, and a runner that finishes anyway believed, since a render already paid for is not thrown away; **an action answered while every lane is full**; an action this package does not know reaching the runner as a queueing that names it, `resumeAnyway` included; a kind with no runner needing attention and keeping its place; a state code nothing has registered still yielding a view; **progress reaching the screen and nowhere in the record, under any name**; the parts a runner gives; the order of the views and the stream of them; the build without the lease showing the jobs and working none until it takes it; a job left *working* by a build that stopped taken up again as a second attempt; a journal that cannot be written not stopping the queue; a runner that throws ending its job with a code and not the scheduler; a job dismissed while it ran stopped, not written back, and its slot given back; a running job left where it is by an action that is not a stop; **what a runner wrote in `stateArgs` surviving the button a person pressed**, through `act` and through `wake`; an action the view never offered doing nothing at all — neither running a finished job again nor lifting the lane's hold — while `dismiss` still works; a lifting press starting the job from the record that carries it; a hold reaching the bytes already moving and taking them up again when the lane is let go — and a job still winding down when the lane goes first going on by itself rather than being stranded, while one that meets a second hold waits under that one and one a **person** paused is left where they left it; every job offering a way off the screen whatever its runner says; a restore not undoing a change the journal refused **by either road**, and the mark clearing the moment a write lands; the stream silent for a list that did not change, and told when a lane is let go even where nothing was requeued; a press a person makes being theirs in **either** order — the hold first or the press first, a stop or a pause — while a second hold still takes over an earlier hold's; a job a hold paused taken up at the next start where its lane is not held, left where it is where it is held again, and a person's pause left for their *Resume* either way; a job a person dismissed not run again when its file would not go, and really gone once it can be; the first list after a release already being the truth; a release touching only the lane it names; a lift pressed on a job whose bytes are moving still letting the lane go; and the actions the scheduler decides walked in both directions against the constant that names them; one timer per waiting job however often the queue is pumped; a build without the lease not rewriting what the holder is working, and a restore not reading back over a job this build is running; `dispose` leaving no timer; a payload holding a credential refused with no job made; **two reasons holding one lane, the first release running nothing and the last taking up what a hold paused; the same reason said twice being one, replaced in place; a lift letting go of the reason that named it and no other; a dismissal a hold left waiting not resurrected by the next film open; a press that lands while a film is opening not read over; a window closed mid-upload leaving the job to be taken up rather than cancelled; and a person's pause carrying no lane's mark, so neither requeue road runs it**; and ids that are never made twice, with the order outliving a restart |
| `packages/filmopen_jobs/test/producer_test.dart` | the producer contract (§6.12) against a stand-in: a run saying where it is, then what it made, and closing; **an item opened as a stream and not an address**, with nothing on it a caller could mistake for one; a failure as a code that says whether asking again might work; what a run writes down read back by its own producer and held to the payload rule, with a producer that cannot say whether a paid submission was accepted answering null; `cancel` and `acknowledge` each asked and neither the other; a run that made nothing; and text with the provenance a batch item records |
| `packages/filmopen_jobs/test/clock_test.dart` | the clock (§6.12), which the scheduler's retries will stand on: a timer firing when its time comes and not before; two due at one instant both firing at that instant; the order being the order they come due; **a timer set by a timer** firing within the same advance when its turn comes, and waiting for the next advance when it is set beyond this one; time never running backwards inside an advance; a cancelled timer never firing, cancelled by itself or by another timer in the same advance; the callback running **inside** the advance, which is where `ManualClock` differs from a real `Timer`, and `SystemClock` running one in a later turn; and both clocks answering UTC |
| `packages/filmopen_jobs/test/journal_test.dart` | the journal (§6.12): a record put, loaded and removed; the order being the order they were asked in whatever order they were written; one file per id; an id that is not a file name refused, a Windows device name among them; **an address or a credential-shaped name in the state's args refused with nothing written, while a code and a lane name pass**; a file of another schema — with a body this build could otherwise read — kept, answered once as `schema` and never deleted, a damaged file of this build's own schema answered as `unreadable`, and a file that is not a job passed over in silence; on disk, a job read back by a second journal over the same folder, a folder that is not there yet, a `.tmp` a killed build left never read and never named by `list`, **two builds writing one job over a hundred and twenty rounds leaving a record that reads every time** — one build's or the other's, never a mixture — a sibling more than an hour from now in either direction swept at the next load while a fresh one is left alone, a write that cannot happen touching no other record, and a removal that takes the file with it; a folder made to fail showing that **one unreadable file costs one file** while a folder that cannot be listed is not an empty one, and that a sweep that could not run never fails a load; the write pinned as a **rename and never a copy** by reading the source, since no behavioural test in plain `dart:io` can tell them apart; and a folder made to fail at a write leaving the journal as it was |
| `packages/filmopen_jobs/test/lease_test.dart` | the lease (§6.12), on a clock of the test's own: a free lease taken and the second build working none; a holder keeping it for as long as it renews; a lease not renewed in thirty seconds taken over, and the old holder learning so at its next renew; `renew` never taking another's however stale; `acquire` on one already ours moving its instant on; a lease given up free at once and another's left alone; a lease file that cannot be parsed being no lease, while one that **cannot be read at all** leaves the holder's alone and this build waits; a folder that throws at a read or a write never leaving a holder believing it still holds one; an instant a day in the future as stale as one long past, and one a second ahead still somebody's; two holders taking a free lease in one instant ending as exactly one; **a holder that stops renewing stopping work without being told, a clock moved back stopping it too, a renewal putting the span back and one that found another's token leaving none behind; a take slower than `stale` answering that it did not hold; a build keeping its own lease for a minute through `startRenewing` with no caller, and stopping the moment another build has taken over**; and the lease file never meeting a load of the journal |
| `packages/filmopen_jobs/test/boundary_test.dart` | the package's own boundary (§4.1, §6.12): its pubspec naming no dependency at all — comments and a one-line flow map read, and `dependency_overrides:` read too — and no `sdk: flutter`; no source importing a package of the application, Flutter, a plugin or the test framework; `dart:io` only in the journal folder's `_io` half; and every file of `lib/src` reached from the barrel |
| `packages/filmopen_state/test/insertion_provider_test.dart` | **the application's side of the service** (§6.13): a working service in every container with nothing arranged and one per container; the container disposing it; a film of this session leaving **nothing** on disk while a film with a folder is written down; the environment answering **no film at all** while one is closed, and the person and the guards Settings has now; and a journal whose folder will not answer **not failing the open of a film** |
| `packages/filmopen_insertion/test/film_journal_test.dart` | where a film's jobs are written (§6.13): a durable film's on disk and a session film's not, both there for this run in the order they were asked in, only the durable one found by the next run, a removal reaching whichever holds it, and what neither could read answered by both |
| `packages/filmopen_insertion/test/producer_insert_test.dart` | **what a producer makes, and what a text proposes** (§6.13), against a stand-in producer since nothing implements the contract yet: three items of which the second is not media leaving two references, two takes and one item to look at, with the run acknowledged; all of them arriving being plainly *done*; an item named `output` with `image/png` named as a take by its type while the batch record keeps the original name; **a stop being a stop for the whole action and not one item missing**; a kind with no producer shown and kept in the journal; a failure said to be final not asked again while a retryable one is; a text written where the place still holds what it held, **and the box reseeded**; a text whose place changed meanwhile replacing nothing, keeping the proposal and offering *replace anyway* beside *dismiss*; that press writing it and taking the run up from what it wrote down; and a proposal the journal will not take — an address in a synopsis — kept beside the record for this run, out of the payload, and answered as `proposalGone` at the next start |
| `packages/filmopen_insertion/test/thumbnail_plan_test.dart` | the ways to make a thumbnail, tried in order (§6.13): a picture decoded and fitted inside 300 × 300; a clip, a picture whose bytes were too large to keep, and bytes that are not a picture at all all falling through to the stock picture; the standard plan answering for **every** kind there is; a plan of the caller's own tried in the order it was given, with nothing after the first answer asked at all; a plan where no way answers saying nothing rather than throwing; and a way being handed the store and a stream as well as the bytes, which is what lets a clip's first frame and a store's own picture be additions made outside the package |
| `packages/filmopen_insertion/test/thumbnailer_test.dart` | the thumbnail (§6.3): a 4000 × 3000 photograph fitted to 300 × 225 inside the budget, a busy one inside it at the lower quality, a tall one fitted on its height, a small one never enlarged, a transparent PNG flattened on the grey rather than on black, an EXIF orientation baked in, a GIF's first frame, bytes that are not a picture making none without an error; the stock pictures at 300 × 300, the same bytes every time and different per kind; a picture's size measured without keeping it; and a 24-megapixel photograph thumbnailed well inside four seconds (s threshold) |
| `packages/filmopen_format/test/media_paths_test.dart` | where a media folder sits (§5.10): a path split and joined on either platform’s separators, a folder resolved against the project folder, the locator written relative where it may be and absolute where it may not, and what makes a folder portable — all of it asked with `windows:` rather than read from the machine the check runs on, so either platform’s paths can be checked from either |
| `packages/filmopen_insertion/test/media_importer_test.dart` | the one way media enters a project (§12.3): a picture becoming a take in the media root, a thumbnail in the project folder and a batch record with `originalName`, `sha256`, `bytes` and the picture's size; two pictures and a clip in one batch numbered per purpose, with the clip wearing the stock picture; a second upload a batch of its own and a second in the same second taking the next free id; a sound taking its purpose from the entity it is dropped on; the refusals — not media (before anything is written), empty, too large, no viewer, a file that is gone by the time the import begins, a name the store already holds making the import take the next id instead, and a store that refuses a name half-way leaving nothing of the batch behind; the URL door against a `MockClient` — fetched and named like any other file, refused for a content type that is not media, refused for a server's answer without its words or the address reaching the exception, and refused for a scheme that is not `http` before anything is asked; an upload found as a take when the project is read again, its thumbnail not an unknown file and its display name the original; and the importer leaving the entity alone, the reference going through `withReferenceAdded`; **and the thumbnail being the plan's, a caller's own plan used and no other way asked** |
| `packages/filmopen_insertion/test/insertion_target_test.dart` | **what a job is aimed at and where its bytes come from** (§6.13): each of the four slots out to JSON and back, and every shape this build does not know — a newer place, a name where the place takes none, a malformed path — read back as **null**; a target of each of the three places round-tripped, and refused for an empty film or stem, a place that is not an object, a place word this build lacks, no kinds, a kind it does not know, and a purpose that is not text; the place key naming the film, the file and the place and **not** whether the block goes first, what it takes or what it is for, with another file of the same film being another place; a durable key told from a session one; files and a producer surviving the journal while bytes and an address do not, an address never saying what it is even to a log; and the payload passing the journal's own check — with the film's key under `film`, since `projectKey` is a name that check refuses |
| `packages/filmopen_insertion/test/replayable_ingest_test.dart` | **the ingest run twice** (§6.13): an attempt given the id the work first chose writing the same names, and one given none choosing a new id — which is the second take the rule prevents; a take landed by an attempt whose taking-back could not happen either, met by the next attempt as its own and kept rather than written again; a take under its name with **other bytes**, and one that cannot be read back, each `nameTaken`; and an earlier attempt's takes left alone when a later one is refused |
| `packages/filmopen_insertion/test/insertion_service_test.dart` | **the service** (§6.13): a reference reaching the file through `patchEntity` and no other road, a `TakesOnly` target writing the take and nothing else, and a job of another film waiting rather than being run against the film on screen; what the journal is told — a files job written down, a bytes job and an address job keeping their source out of it (and the address nowhere in the payload), and a film with no folder being a film of this session whatever its source; the batch id in the **payload** before a byte moves and the take wearing it; `jobsFor` answering one place and no other; **each reference put where its own kind belongs — a picture to the array, a clip to `motion`, a character's sound to `voice.refs` — with the first picture proposed as the `preview` and one a person set never changed, a refused attach reaching the same places again without the store being asked, and a shape that will not take one said as `placeUnwritable`**; a target this build cannot read held as `targetUnreadable` with nothing written, a file that is gone held as `changedMeanwhile`, a refused write coming back as the door's own code, and a film closed meanwhile leaving the job **waiting** rather than failed; **a refused attach attached again with no second `put` and no second read, while a job whose ingest never finished is not offered the button; a place whose shape will not take a reference said as `placeUnwritable` with the take safe, while a refusal that is really the writer's comes back as the writer's; a store's own word carried out as `store.drive.refused` without this package knowing whose; and a failure that keeps happening given to a person after the bound the runner names, with a person's press beginning the count again**; and the no-op default writing nothing |
| `packages/filmopen_insertion/test/boundary_test.dart` | **the new package's boundary** (§4.1, §6.13), read from the files themselves: its pubspec naming only `filmopen_format`, `filmopen_jobs`, `filmopen_media`, `filmopen_store` and the four third-party rows, and no Flutter, with **`dependency_overrides:` read too** — an override reaches the same resolution as a dependency, and the clause was lost once already in adapting the test; no file under `lib/` importing `package:flutter/` or another FilmOpen package's `lib/src/`; `dart:io` only in `local_file_io.dart` and `dart:isolate` only in `off_thread_io.dart`; and the barrel exporting every file but the two that are deliberately internal and their `_io`/`_stub` halves |
| `packages/filmopen_media/test/media_cache_test.dart` | the cache a fetching store puts its bytes in: a file kept under a name made from the **whole** media path with its extension — so `stills/front.png` and `posters/front.png` are two files and not one — none answered for a file it has not, a file already there kept rather than fetched again with the answer drained so no connection is left open, the budget held by dropping the oldest first with what was just asked for kept even when it fills the budget alone, and a folder from an earlier session evicted rather than growing for ever |
| `packages/filmopen_media_ui/test/media_root_picker_test.dart` (the source rows) | a source’s panel replacing the local row **and its sentence**, which describes a box a source does not have, and the kind pull-down drawing the source’s own icon |
| `packages/filmopen_media_ui/test/media_root_picker_test.dart` | where a new project's media will live: the path proposed as `<parent>/<tag>-data` and following the tag until the person types one of their own, an empty row meaning no locator, Browse putting the chosen folder in the row and a cancelled dialog changing nothing, the identifiers once each with no kind pull-down while there is nothing to choose between, a registered source adding its kind and its own panel, the Spanish sentence, and no overflow at 380 px in Spanish and dark |
| `packages/filmopen_state/test/patch_entity_test.dart` | **the one door a change made below the forms goes through** (§7): a change asked for in another film writing nothing and answering `ProjectNotOpenException` with its code, the **key** compared and not the index so a re-read of the same film is still that film, and a `session:` key of no open film being nobody's; through the writer where no form is open and through the **draft** where one is — what is half typed going out with the change, the key named for the draft's observers, and a key the change dropped dropped from the draft too; the boxes asked for back only when the change asks; the change handed neither the index nor the live draft, each proved by scribbling on what it was handed; every refusal of the writer — no handle, another author's file, a project nobody may write, a file changed since it was read, content the writer will not save — answered as itself, as its `writeProblemCode` and with nothing written; **the viewer being the one passed in and not the one Settings holds**, either way round; a file gone from the index, and a write that landed without the file holding what was asked for, each `EntityChangedMeanwhileException`, which a caller may name for itself; a change that changes nothing writing nothing — through a draft or without one — and succeeding, and still being read back from the **file** and not the draft, so that a value only typed is written by settling that draft before it is called done; a file gone while a clean draft still stands for it refused rather than crashed; a change through a draft written by the form's saver and so refused, never misattributed, when the handle changes under it; every refusal the door answers rather than throws logged by its own word, with no message travelling; the caller's `change` asked **once** on the writer road — it is asked twice where a form has a draft open, against what that form would write and then against the live draft, which may have taken a keystroke in between, so **`change` must be a pure function of what it is handed** and a value of its own must be made before it, not inside it; a file that went while the write was landing refused whatever `holds` would say of nothing; only **this** file's draft settled on the unchanged road, a form on another file left to its own debounce, and a write no form waits for any more still waited for, since it may be landing on this very file; and **one change at a time per file** — two asked together both landing, a third asked while the second is still writing taking its turn behind it, a change to another file not waiting at all, and neither a refusal nor a change that throws stranding the one behind it; and **a write no form is waiting for any more waited for** on the unchanged road, since it may be landing on this very file |
| `test/patch_reseed_test.dart` | **a text written from below takes its place in the box** (§7, §9.3's): the project's form open in *Edit* with a word half typed in another box, a change made through `patchEntity` with `reseed` — the box taking the value, the half-typed word neither eaten nor lost (it goes out with the change), and nothing else moved — and the same change without it leaving every box as it was, though the file is written either way |
| `packages/filmopen_state/test/media_provider_test.dart` | `mediaStoreProvider`: a local root answering a store of the folder the manifest names, a project that names none being its own, a locator type no source serves reported as `unsupported` with the project still open, a folder the loader could not open reported as `unavailable`, a registered source answering for its own type and for no other, and no store while no project is open |
| `test/media_section_test.dart` | the reference section on the whole app: a picture fetched through the URL door against a `MockClient` becoming a tile, a reference in the file, a thumbnail in the project folder and a batch record; a second upload appending with a batch of its own and the first still there; a clip joining `motion` and wearing the stock picture with no `preview` proposed; no **+** on somebody else's version and none without a handle; a tile opening the picture at full resolution and closing; the media keys gone from *Other attributes*; the Takes tab listing the upload; Compare pairing the card and a `<` adding a reference without removing one; **Browse** adding two real files at once through the sheet's own door with the picker injected, both reaching the file in one batch; a media root of a kind no source serves leaving **+** there but disabled with its reason in its tooltip; a refusal staying inline under the strip in the person's words with nothing written; and no overflow at 700 and 532 px in Spanish and dark |
| `packages/filmopen_media_ui/test/player_controls_test.dart` | the row a person presses, pumped without a decoder: one control named for what pressing it does next and calling back once; the time as where the file is over how long it is; a drag asking for **no** seek until the finger lifts and exactly one then; the thumb staying where it was let go until the player arrives, and given back after two seconds when it never does; a file of unknown length with the bar disabled, its reason in the tooltip and its clock still counting; and the row at 700 px in Spanish |
| `packages/filmopen_media_ui/test/player_rules_test.dart` | what the player draws, as arithmetic a check can reach without a decoder (`player_rules.dart`): the ratio a file that reports none is drawn at, whether there is anywhere to seek to, whether the file has run out — the last fifth of a second counting as the end — and the clock, `m:ss` under an hour and `h:mm:ss` from exactly one |
| `test/media_section_test.dart` (the location's cases) | the same card on a location from one line (step 4): a picture of the café named `cs_lo_cafe_…` and becoming the preview, with its take in the media root and its thumbnail in the project folder; a sound named `am_…` and joining the `sound` group, with no `voice` on a place; Compare pairing the card and `<` adding without removing; and 700 px in Spanish and dark |
| `test/media_section_test.dart` (the player's own cases) | the viewer asking the platform for a player and drawing what it answers, with `player.play` beside `player.close` and the file's kind above them; and a platform that has none saying so, with no `player.reveal` for media that is on nobody's disk; the dialog and its controls row fitting at **700 px**, the seek bar taking what is left and the time beside it. The player itself is the platform's, so a check stands in for it (`debugMediaPlayer`); the real one is exercised by `scripts/agent_walk.py` on Windows, which also opens a file whose bytes are not a clip and expects the error state and `player.unplayable` in the log |
| `packages/filmopen_drive/test/drive_android_flow_test.dart` | the **whole** Android consent flow composed, driven without a browser, a device or a socket (§6.10, s M2): the consent page opened with the Android client and its redirect answered and exchanged; the verifier sent with the code being the one the challenge was made from; a browser that will not open being *unavailable* with nothing exchanged; a build given no link door saying so **without** sending the person to Google; a redirect for somebody else's request refused with its code never exchanged; a refusal from Google kept as a code with none of its words; and a state and a verifier of their own for every sign-in |
| `packages/filmopen_drive/test/drive_android_client_test.dart` | Google Drive on Android (§6.10), read without a browser, a device or an account: which client each platform signs in with and that a build carrying one is unconfigured on the other platform; that an Android client sends **no** `client_secret` rather than an empty one; the redirect scheme as Google's reversed client id, computed the same way `android/app/build.gradle.kts` computes the manifest's placeholder, and an id of an unexpected shape yielding a scheme rather than a crash; the Android consent address carrying everything the desktop one does — the scope, `access_type=offline`, `prompt=consent`, the S256 challenge, the state — with the custom scheme as its redirect and no secret anywhere; and the **refresh** request's own body, where the wrong secret would not show until the access token ran out an hour later: the Android client and no `client_secret` key at all, the desktop client with its secret unchanged, an expiry kept, a refresh token not invented, and a refusal that is a code and never the server's words; the **code exchange**'s own body, which is FilmOpen's request and not the library's, carrying no `client_secret` key on Android and the secret on a desktop, answering tokens from a stand-in, and turning a captive portal's page into *unavailable*; the redirect waiter driven from a stream — our own scheme only, a wrong state refused before the code is used, an error carrying no code, a bounded wait, and the subscription given back however it ends; and the **Kotlin read from `android/app/build.gradle.kts`**, so the two computations of the scheme cannot drift apart unnoticed |
| `packages/filmopen_drive/test/drive_consent_test.dart` | what FilmOpen asks Google for, read without a browser or an account (§6.10): Google’s own endpoint and the loopback redirect; **`access_type=offline`** and `prompt=consent`, without which no refresh token is ever issued and the application stops working an hour after every sign-in; the one scope; PKCE’s S256 challenge against RFC 7636’s own test vector; a verifier and a state that are long, unreserved and never the same twice; and no client secret anywhere in the address |
| `packages/filmopen_drive/test/drive_media_store_test.dart` | Drive against a stand-in in memory (`fake_drive.dart`) — **no account, no token, no request that leaves the machine**: a build with no Google client refusing as `unconfigured` and the project opening all the same; nobody signed in as `refused`; signing out removing the keys; an access token that has run out refreshed and the refresh token kept; a root made under a parent and the locator answered as a path; the folder made by the first upload, not the dialog; the id remembered so the path is walked once; the parents offered being FilmOpen’s own folders and nothing else of the person’s; a locator naming no folder refused rather than read as the whole Drive, and a remembered id walked again when its folder has gone; an upload named and placed, a second of the same name refused with nothing replaced, one whose size nobody knows refused before anything is written; a fetch into the cache once and a file on this machine the second time; and every refusal a code, with Google’s own words never coming back with it |
| `packages/filmopen_drive/test/drive_source_ui_test.dart` (a build with only the desktop client says *no Google client* on a phone, and one with the Android client offers to sign in there) | the panel: a build with no client saying so and offering nothing; the scope sentence before any browser opens; signing in showing the parent and the name and settling a locator; a name of the person’s own; signing out forgetting the keys, saying what it does not do and leaving no place settled; a refusal said in FilmOpen’s words, with no address in it, and the button still there to try again; and 700 px in Spanish |
| `test/drive_source_test.dart` | the application with a second source registered, which is the whole of what registering one does: *New project* growing a kind pull-down and showing Drive’s panel in place of the local row; **Create waiting** while that panel has settled on no place — the form filled first, so that nothing else holds it back — and saying why, and going live again when the local folder is chosen; a project whose locator is `google-drive` with nobody signed in refusing at the moment of upload, in FilmOpen’s words, with nothing fetched; the upload sheet offering no Drive door; and the registry answering the local source first |
| `test/sample_media_test.dart` | the bundled sample as a session copy: its media root is an overlay that reads the bundle and writes to memory, a second copy starts from the bundle again, the bundled folder itself refuses a write and answers a length, and a take written into a session copy's media root is loaded as a take of the project |
| `packages/filmopen_state/test/localization_test.dart` | Spanish messages, field labels, diagnostics and tree text, and a dotted leaf path read as words in both languages |
| `packages/filmopen_format/test/flat_json_test.dart` | the flattening both Compare and the generic View read (§13.3): dotted paths, a list kept whole, a top-level-only skip, an empty object contributing no path, and that what a value row never shows is the header keys less `name` plus `blocks` |
| `test/responsive_test.dart` (a 360 dp phone and the Galaxy's 411 × 891 among the drawer sizes and the overflow widths, on a phone the system's back closes the tree and the page stays, Settings' segmented buttons keep every label on one line at 360, 375, 411, 532 and 1200 px in Spanish, with the icons gone at the three phone widths and kept at 532 and 1200) | the two layouts (§9.1): the tree away and behind an icon below 700 px of window with the rail still beside the drawer, choosing a row navigating and closing it while opening a branch does neither, the tree back on the page at 700 px and above, the remembered sidebar width seeding a **fresh** split after a trip to the drawer, one field to a row below 560 px of pane — checked at a width where the fields would otherwise have shared one, since below about 344 px they would not have fitted anyway — a comparison that keeps two columns, a 28 px gutter and rows with height at 375, 532, 700 and 1200 px without ever scrolling sideways, **nothing overflowing at any of twelve widths from 180 to 1200 px in Spanish and dark** (the language whose sentences are longer, which is where two of this step's failures showed), the tree's header keeping a readable project name at the sidebar's own minimum with the handle moved under it, a label going above its chips where beside would crush them, a comparison's headings travelling with its rows where the page is short, and **the header above the tabs never taking the height the tabs keep**, with the bar that says it was cut |
| `packages/filmopen_format/test/field_paths_test.dart` | a form's box on a nested key: the text it starts with (a string, a number's digits, a list one per line), creating the objects on the way and leaving siblings alone, an emptied box removing its key and every object left empty, clearing what is not there changing nothing, lines and numbers (text that is not one writing nothing), never writing into the document it was copied from, and a value of another shape — an object on the way that is not one, an object where a word goes, a list holding an object — left alone |
| `test/character_location_form_test.dart` | the forms: every key of A.1 and A.3 owned by a field (collected over the whole scrolled page), each section with its icon, a box named without its object, the media keys left in *Other attributes*; typing a nested key writes it beside its siblings and emptying the last key of an object removes the object; a draft observer (the recorder's seam) hearing a chip's token, a box's trimmed text and a relationship row's list; the voice chips choosing, a second tap removing, an agent typing a token through `FieldRegistry`; relationships added (one untyped row at a time) and removed with the row below moving up; a value of another shape locked with the reason, written over by nothing and shown in *Other attributes*; the location's kind, time of day and lines written; *Located within* listing the other locations by name and not itself, writing the tag, an agent's tag that is no other location writing nothing, *None* removing it, and a tag no location has kept and said; another author's version with every field and no chips or row controls; Compare pairing the new sections and copying a nested key left without removing a sibling; nothing overflowing at 532 and 700 px in Spanish and dark, scrolled to the foot of both forms |
| `test/plugin_sections_test.dart` | a plug-in's card on a character (§9.4): a text box, a pull-down and a switch typed through `FieldRegistry` and autosaved under `plugins.<tag>.<key>` of the version being edited, another plug-in's data kept, a token the pull-down does not offer and a switch that is not true or false writing nothing, an emptied box removing its key; the card locked and legible on another author's version; Compare pairing the card and copying a value left; 700 px |
| `test/plugin_hook_test.dart` | *Plugins…* on a page (§9.3): a scene's file menu running the template's rewrite on the version shown, its progress and *Stop*, *Written: …* and a 44 px *Open it* showing the new version; *Stop*, and closing the dialog, cancelling the run; the item absent on a type no plug-in rewrites; a refusal said in words at 700 px |
| `test/generic_view_test.dart` | View for a type with no form (a style when a location got a form): a scene's story epoch as a value row and a location's filename epoch not, every leaf path the Overview tab reached is still reachable over the whole sample (walked independently of the code under test), a scene's blocks as rows against a cue's `blocks` as a value, values rendered rather than printed as JSON, the File and header-disagreement cards, an empty object surviving in *Other attributes*, the file menu's editor and clipboard, the pending draft written before the editor opens, and no overflow at 532 and 700 px in both languages (a width `pumpApp` now honours — it used to fix 1600×1000 over whatever the caller had set, so both of this table's width claims were made at 1600 until) |
| `test/compare_test.dart` | two-column Compare (§13.3): the viewer's latest on the left and the selected version on the right with their rows level, a key only one version has saying so rather than leaving a gap, the three reasons the tab refuses to open, the question a reader with no version of their own is asked and both its answers — *No* writing nothing and *Yes* forking the version on screen without moving the page — and that nothing compares across epochs |
| `packages/filmopen_forms/test/draft_saver_test.dart` | the autosave on its own (§9.3): nothing written until the draft has been still for the 700 ms the form uses, one write at a time with a keystroke during a write keeping the draft dirty and *Saving…* observable while a deliberately slow write is in flight, a change typed during a write whose form is then disposed written once against the text the writer handed back, and tried once more against the text it was read with when that write fails, `settle` waiting for the write in flight and writing what came after, `settleOpen` answering false while one draft is refused, forgetting a disposed saver and waiting for the write it left behind, `adopt` taking back only the draft's own write, a re-read holding the write in flight adopted, a nested change never reaching the file's JSON, a change that changes nothing writing nothing, an outside change refused while the draft is dirty and the same re-read adopted when it is clean, and a pending draft written when the form moves to another file or is disposed; the draft observer (the issue recorder's seam) hearing the trimmed text, a chip's token, a relationship list, a removed key as null, a copy-left's several keys with no value, and nothing for a change that changes nothing; and, a change from below the forms (`OpenDraft.apply`, §7.1): the registry finding the saver by the file it is on and following it to another file, the last registered of two answering and a disposed one no longer, the change written now with what was typed and not yet saved, a write in flight waited for with what was typed during it going out too and no third write, a refused write as the answer with the draft keeping the change, an older failure not making a later success a refusal, and the observers hearing the key |
| `packages/filmopen_state/test/plugin_roles_test.dart` | **the seam by role** (§7.1): the runtime in use is every role and the base they share; a class that implements `PluginInvocation` alone **is not** a catalogue, a settings control or a bench, and still calls; **the members of each role, read out of the interfaces themselves**, so a member that moves between roles — or down to the base, where every caller would inherit it — fails here, and the union declares nothing of its own but `attach`; **each package names only the roles it uses**, read from its own sources outside its comments, so a call site that reaches for `PluginRuntime` again fails here; and the no-op runtime answers every role without a plug-in — the app's own key owner and nobody else's |
| `packages/filmopen_state/test/project_provider_test.dart` | the controller's re-read: of two overlapping re-reads the one started last wins whatever order they finish in, the overtaken one answers false and leaves the base index alone, and opening another folder drops a re-read of the previous one still in flight; the sample opening as a session copy everywhere, in both modes; `openSample` in both modes; closing writing the open drafts and the next open ending the closed state; *Copy to a folder* on a temp folder — the files written into a folder named after the project and the project opened there — a folder that already holds files refused, with nothing written and the session copy still open, a copy failing part-way taking its folder back with nothing escaping it, a target that is a link refused, with a sentence of its own and nothing written through it, and a project named after a Windows device name copied into `project`; and **`projectSourceKeyProvider` being the format's `projectKeyOf` and nothing of its own** (§5.15) — no project answering none, two session copies of one label being two films, and a re-read of one source keeping the key |
| `packages/filmopen_store/test/copy_left_test.dart` | — and the reference section's own copy: what the left lacks added and nothing removed, the right version's grouping kept, a character's `voice.refs` copied without the rest of the voice, `preview` copied as any other word and only where it differs, nothing offered when the left already names everything, the keys named for the message and the log, a value of another shape left alone, and a reference the right names in another group not added twice | copy-left's rules without a widget (§13.3): the keys that never move — per file: `epoch` only where the filename carries it, `by` and `source` always, a project's `contributors` — a row reaching the roster or the provenance offering nothing, a scene's story epoch copying where a location's does not, the epochs a scene's content, its `locationEpoch` and its cast and props entries put in use (once per file), a key only the left has neither offered nor removed, an equal value — and one written in another key order — not offered, an empty value of any kind never written over one that is not, a path through a shape the two versions disagree about refused, a subtree and a list copied, a path written without touching the object the index holds, nothing shared with the version copied from, §13.3's four block cases with the insert landing where `mergeOrder` drew the row, a duplicated block id offering nothing, and the epochs a copy would drop while files are at them |
| `test/compare_copy_test.dart` | copy-left on screen: a row copy writing one key and leaving identity, the dates and unknown keys alone with the version copied from untouched, a section and copy-all as **one** write each (counted in the log) that never carry a key copy-left will not move, the rows that offer no `<` proved beside one that does, an epoch in use refusing both a row copy and copy-all with the file unchanged and a sentence naming the way round it, a copy the writer would refuse refused first in its words with an empty name never offered, a scene's story epoch copying, a project's contributors shown and copied neither by row nor by copy-all, a rebuild in the other theme — its controls taking the new colours — asking no row again while a copy does, a scene's blocks replaced, inserted and deleted by id with copy-all deleting none, a scene whose blocks are ids compared as a value on both sides, the §13.2 banner over an official left column and its Fork, which forks the left version after writing a copy still pending, the message naming the row it copied, the icons on the split at the size the gutter allows, every control disabled with its reason on a read-only project, and the sample's own scene and location — in a writable copy, so `assets/sample/` is never edited |
| `test/widget_test.dart` | the app: overview and tree, the tab set (no Overview, no JSON) and every entity tab laying out without overflow, the commentary feed ordered by instant across offsets (§1.4), tree highlight when choosing a version, navigation from the detail pane reveals the row, the project chooser |
| `test/folders_tree_test.dart` | the Script branch on folders in the running app (§8.2): the sample's seasons, episodes and sequence as folder rows with the folder icon and their kind in words, in English and in Spanish, every row name first, the episode's cue under its folder and no *Not in the script*; a scene row opening its page with its own node id, and a row's stem as its tooltip (§8.3); on `foldersFixtureFiles` each report as a flagged row with its sentence, a kind the vocabulary lacks as written, a folder without a kind without a detail, and *Not in the script* with its count; a row four folders deep drawn inside the pane with no overflow at 700 px beside the page and at 375 px in the drawer, light and dark; and an entry that spells another row's id (`fo_f1/sc:1`) getting an id of its own, every id in the model naming one row |
| `test/folders_add_remove_test.dart` | the row menu and the dialog in the running app (§9.2): *Add folder…* by a right click on Script — the name above the short name, `f3` proposed, the kind with *None*, no epoch, the first-file note, the file and the pick written, the entry at the end of the project file's list, the folder's page opened, the message; *Add scene…* from the ⋯ of a selected folder — the ⋯ on no unselected row, the next number, no kind, an unnamed scene named *Scene 2*, one parent written; the ⋯ under the pointer, and a taken short name refused in the dialog with nothing written; on another author's folder, without a handle and as another author on the Script row the items disabled with their reasons, *Remove* still offered where the list is the viewer's; *Remove from the script* moving the row to *Not in the script*, deleting nothing, and offering no *Remove* there; **the project file's page open with its logline half typed** — a change of the script writing both, the box not reset (its trailing space kept), and the next keystroke saved and not refused as stale with the old list not written back; the menu and the dialog at 700 px and in the 375 px drawer, light and dark; a short name a list names proposed past and refused as taken, a typed short name kept whole in an unnamed folder's and scene's name; the highlight following a removed row to *Not in the script*; and the dialog's longest sentence read in full at 375 px, in Spanish too |
| `test/script_page_test.dart` | the Script page and a folder's page (§9.3): choosing Script — the selection and the row's id, the Contents card with its two buttons, every row in the tree's words (a folder its kind, a scene its short name, a bare tag its reason), *Not in the script* as a second card whose rows have no menu, a row opening its page; an empty script saying what would fill it, and the card's *Add scene…* opening the same dialog, writing the same files and opening the new page; a row's menu on the card removing the entry with nothing deleted and *Add* not in it; a folder's page — the card above the form, Name, Kind, Synopsis and Notes by their identifiers, no *Other attributes* and no second *Notes* card, the vocabulary as chips, a typed synopsis and a chosen chip autosaved with the list untouched; another author's version — its kind as written, its own list, the buttons disabled with *Fork this folder first…* and a tap doing nothing; Compare with `children` as one row and without the card; a plug-in's card on a folder and its field's identifier; the agent's `{"kind": "script"}` opening the page and `state.selection` answering it; both pages at 700 px and 375 px, light and dark, English and Spanish, the buttons and the rows inside the window and a button's label on one line; the unfiled card's rows flagged and the script's own not; the disabled buttons' reason on the node that carries their identifier; an add into a version of one's own that is not in use — the entry in that version's list, the version in use untouched, and the message saying where the scene went; the chips' line saying that a folder's kind decides nothing; the list called *Script* in a removal's message; a row's identifier as its entry, the second of two equal ones with `#2`, and an entry that writes `#` or `%` with their codes, each found once |
| `test/scene_form_test.dart` | the scene's form (§9.3): a scene opening on *Edit* with Name, Synopsis, Location, Epoch, Characters and Notes in that order, the blocks as rows and *Other attributes* after them, no generic *Attributes* card; a location and an epoch chosen and written, *None* removing the key and nothing else of the file changing; a character removed and added — the menu offering only those not in the scene, the entry that stays keeping its `epoch` and `outfit`, the last one out removing the key; a character's own epoch where the project declares two, an epoch the project relabels relabelled on the row, *As the scene* removing the entry's `epoch` with the outfit kept, and no such pull-down in a project with one epoch, where an entry's epoch is kept; a location, an epoch and a character the project does not have shown as written and said, on the viewer's own version and locked on another's; a `cast` of bare names drawn as it is and never written; an agent typing into every field, `cast` one short name per line with the entries that stay intact, a name nothing has refusing the text, nothing removing the key; Compare pairing the six rows level and the blocks by their ids with their copy controls; a plug-in's card and field on a scene; 700 px and 375 px, light and dark, English and Spanish, every control inside the window and none crushed; a cast row's epoch labelled *Character's epoch*; a scene without a block saying on its *Script* tab where its words are written today |
| `test/new_project_test.dart` | a new project (§9.2): the dialog asking the title first, with the focus, and no kind; a title typed first filling the short name with its nearest segment, cut to its length, and the rest of the dialog following it; a short name typed after it, and one emptied, no longer followed by the title; a short name typed first left alone; *Create* on a real temp folder, in English and in Spanish — the manifest, a project file without a `kind` whose `children` is `["sc_1"]`, `sc_1_<creator>_v1.json` named *Scene 1* / *Escena 1*, the app on that scene, on *Edit*, its row under Script and nothing unfiled; the project's page setting the kind, clearing it with *None* and leaving `children` alone, an agent typing a kind, a word that is none and nothing; a kind this app does not know staying as written; a title of signs leaving *Create* disabled with the short name's problem as its tooltip, and a followed name cleared with its title saying it is missing; a legacy spelling typed by an agent written as the kind it means, a legacy spelling in the file shown as that kind and kept when the same entry is chosen again, and a `kind` that is a number drawn locked with nothing writing over it; the disabled *Create*'s reason naming its box — the folder, the short name, the username; an empty `kind` reading as *None* with no blank entry and removed by *None*; a legacy spelling overwritten by the kind's own name once the kind is really changed |
| `test/folders_move_test.dart` | moving (§9.2): *Move up* and *Move down* reordering the tree with one write of the list and nothing else changed, disabled at the ends with the reason; *Move to…* listing Script and the viewer's own folders indented as the tree, without somebody else's folder and with the line that counts it, *Move* waiting for a choice, a pinned entry put, still pinned, at the end of the folder chosen, and the message naming it; a folder's own branch absent from its own dialog, *Cancel* moving nothing, a folder moved into another; a scene beside the script filed by *Move to…*, its only item, from the tree and from the Script page's second card; a move whose second write fails saying the row is listed twice, both lists holding it and the second flagged; a Contents card's row menu with the same four items doing the same; every item disabled with *fork* as the reason in a list that is somebody else's; a viewer with one folder of their own offered that one, a viewer with none told what would give somewhere; the dialog at 375 px, light and dark, English and Spanish; Script left out by name with its author and the folders counted apart from it; without a username *Move to…* on a row beside the script disabled with that reason and no dialog |
| `test/folders_drag_test.dart` | drag and drop in the tree (§8.3, §9.2), with a mouse unless said: a scene dragged onto a folder its last child, the message naming the folder, nothing deleted; dropped on a row's upper or lower edge before or after that row — within one list downwards and upwards, into another list, and on a folder's own edges in its parent's list; **a drop writing what the same move by menu writes**, file for file; the folder under the drag highlighted, an edge showing one line, somebody else's folder showing nothing and a drop there saying to fork and moving nothing; a folder into itself or beneath itself and a row dropped on *Characters* moving nothing and saying why, a row on itself saying nothing; a closed folder opening after 700 ms under a resting drag and not before, then taking the drop among what it holds; the list scrolling near its bottom, stopping away from the edge, and scrolling back near its top; **a finger's long press** starting the drag, a chevron still toggling and a tap still selecting, at 700 px in the pane and at 375 px in the drawer; what is somebody else's not picked up however far the press goes, and a row beside the script filed by a drop; the list scrolling under a pointer that slides along the edge as under one that holds still; **a drag that scrolled its own row out of being** still writing its drop and letting the list go — without the fix the list is pulled back 16 ms after it is put anywhere; a closed folder not opening under a drag that keeps moving over it for 1.5 s; a finger reaching a folder's upper third; a row the project took away under the drag said and nothing written; a row dropped back where it already is writing nothing and saying nothing |
| `test/folders_review_test.dart` | what an adversarial review found on the screen, each as the test that would have caught it (§9.2, §9.3): the move dialog offering no folder the move would refuse — a folder the moved one holds whose first listing the tree draws outside its branch, and a destination that names the row already said, in a message that is still there after six seconds and gone after eleven (`kLongMessage`); in development mode a change of somebody else's list from the tree said in the confirmation's own message, and nothing added on a list of one's own; a locked, empty Contents card saying why as text under its sentence; and *New project* refused on a folder that holds files — real I/O in a temp folder — putting the username back, the folder as it was |
| `packages/filmopen_store/test/project_writer_test.dart` (a plug-in run) | a returned entity written as `<viewer>.<tag>`'s v1 with `by`, `forkedFrom`, no label and its plugin batch, the person's version untouched, a second run v2; an entity matching no source, content the format refuses and no viewer writing nothing; a run whose second write, or whose batch, is refused leaving nothing of it; a plugin batch under the viewer's derived handle for that plug-in accepted, an import under it, a plugin batch under another person's and one under a workspace that is not the plug-in's refused; and a render's request recorded in **each take's item** beside what the file measured, with nothing of it at the batch's top |
| `packages/filmopen_store/test/manifest_test.dart` | a project's plug-in folder (§4.2): its manifest indexed and its code a companion file by their names, its locale file, README, platform file and asset not reported, `_inbox/` still reported; the manifest and media root (§4.4): locators read by `kind` and written with it, 1.11's `type` unread and a manifest that spells it reported as malformed with no media root opened, takes found in the media root and beside the JSON, no manifest, unsupported kind, missing folder, absolute path, malformed `data`, tag mismatch, a manifest missing `filmopen` or `tag` |
| `packages/filmopen_store/test/library_test.dart` | the shared library (§14.5): entries marked by origin, separate counts, project stem shadows the library's with a warning and none of the replaced file's warnings survive, a fork beside library versions, a project official pointer overriding silently, an unreadable library folder as a warning rather than a failure |
| `packages/filmopen_format/test/short_name_test.dart` | the segment rule in one place: validation, a handle's two segments and the reserved owner, Windows device names, the ten-character advice, normalisation, and that the filename grammar warns by the same rule |
| `packages/filmopen_store/test/app_log_test.dart` | JSON-line shape, thresholds, verbose and the environment pin, a failing sink, unencodable data, `timed`, the file sink and its rotation, an error quoting a text box's value keeping no typed text |
| `packages/filmopen_forms/test/page_access_test.dart` | what a page may do and which red lines it shows (§9.5), without a widget: enforcing — the viewer's own file, the star for directors, the library locked, no line; development mode — another author's version editable with its line, the star for everyone with the directors' line, a library file editable where the library can be written and, where it cannot, not editable with its own line, no handle writing nothing and saying so, a folder the system refuses; `directorHandles`, and the directors' sentence naming them or saying the project names none |
| `test/development_mode_test.dart` | development mode on screen (§9.5): another author's version opening in *Edit* with its red line and what is typed written under its author; the star pressed by someone who is not a director, writing the pointer and saying the directors' sentence again; a label written on another author's version under its author, the message repeating the line; Compare opening on the viewer's own latest with its reason as a red line; no handle leading to Settings; a session copy's line offering *Copy to a folder…*; and, enforcing, no red line, *View*, and the star disabled |
| `packages/filmopen_store/test/guards_test.dart` | development mode on an in-memory project (§9.5), one group per row of the plan's table, each enforced and in development mode: another author's version (refused; written under its own author with the override logged at warning level, with its data), official without being a director (refused; set and cleared, two overrides), no handle (refused in both), a read-only folder (refused in both), the shared library (refused; written into the library and not into the project, and both overrides logged for another author's library file; a read-only library and a library file without a handle refusing with no override logged) — and the rules that protect files refusing in development mode too, with no override logged: a changed identity, missing content, a project file's `forkedFrom`, a stale file |
| `packages/filmopen_state/test/settings_test.dart` | recent projects (tolerant parsing, order, no duplicates, cap), library path and verbose defaults, and the viewer being the handle only when it is one — anything else, `official` included, browses as nobody; development mode's default taken from the build and its setter deciding `Settings.guards`, which a build without the mode ignores; dictation's microphone and *Hold to record*, their defaults, a device cleared back to the default, and a log that never names a device |
| `packages/filmopen_state/test/build_stamp_test.dart` | the build's stamp (§6.6a): a CI build and a local one, two local builds a second apart two stamps, a build with no moment, seven moments that are not an instant in UTC to the second left out (a space, a zone, a fraction, 31 February, 24:00, a word, a tail), defines of any length and any character giving printable ASCII within 120 and a version within 40, a full commit hash fitting, and the platform in lower case |
| `test/about_stamp_test.dart` | the build's stamp on the whole app at 700 px (§6.6a): Settings → About and the support dialog's *About* tab each show `FilmOpen <stamp>` as selectable text, the bare version gone; the stamp of a build without a script is the version with `dev`, `local` and the platform |
| `packages/filmopen_store/test/project_creator_test.dart` | creating a project on a real temp folder: the manifest, the first version (director, one epoch, **no `kind`**, `children` of `["sc_1"]`) and the first scene — its keys, its name as the caller gave it, in use without a pick and nothing unfiled, a scene name of nothing but space falling back to the short name and a workspace writing as itself, refusing a non-empty folder and a bad tag; deleting a project folder with everything in it, a folder with a project file and no manifest counting as one, and a folder without a project at its top (one deeper down does not count), a missing folder, a file and a link refused with nothing touched; and the media folder of a new project (§4.4): made, named relative to the project folder when it sits beside or under it and absolutely otherwise (which the loader then reports), an empty row writing no locator and making nothing, a locator a source made for itself written as it is, and the path arithmetic on Windows; **a project that could not be finished taken back** (review) — an author that is no handle refused where the file names are built, leaving no folder, an empty folder the person had made left empty and then usable, a media folder inside the project folder gone with it and one beside it left, **a link to an empty folder still a link** with the folder it points at empty again and then usable, and `writeNewProject` refusing the handle itself before it writes anything |
| `packages/filmopen_store/test/project_writer_test.dart` | the writer on an in-memory project (§13, §17): save keeps unknown fields and stamps `updated`, refuses other authors, no viewer and read-only sources; fork numbering for own and others' versions, the fork identical apart from the header and without a version label under either spelling, a project fork adding its author to `contributors`, the fork becoming the pick; the identity lock and a repair towards the filename, a stale file refused, a duplicate key refused at read, content validation, only a director writing the pointer; a new character or location written as the viewer's v1 with its name and kind, beside a project file kept in a subfolder, recorded as the creator's pick with nothing official, and refused for a tag the type or the library has, a bad tag or epoch, an undeclared epoch, a type without epochs, no handle, no name and a read-only folder, with nothing written; official set (rewriting the pointer, dropping its note), new pointer, clear; the pick order (pick, official, own, sole owner), the date a pick carries and abandoning one, pinning versus tracking official, a missing or other-epoch `pick` with its warning, per-epoch picks, the folding of an older stem and of the plain date beside it, workspace handles inheriting the owner's pick, the project file following §4.1 unless a pick says otherwise; epoch order, re-encoding, default-epoch fallback, the no-epochs warning; kind vocabulary; and, a new folder and a new scene — the viewer's v1 with no epoch in its name, a folder's kind, the pick as a plain stem, no `children` written and nothing listing the file until the caller does, beside the project file in a subfolder, refused for a taken short name, an epoch, any other type, a bad short name, no handle and no name with nothing written, and a file of that name that appeared meanwhile refused and never replaced |
| `packages/filmopen_format/test/character_age_test.dart` | `birthdate` as a year, text or calendar date (impossible dates refused); the proposed age, the override, its removal |
| `test/editing_test.dart` | editing through the screen on an in-memory project: Settings' handle box explaining a reserved or malformed handle and taking an empty one, Settings → Development switching the provider's writer from refusing another author's version to saving it with the override logged, a pick naming a missing file removed with the check on the official version, Fork carrying what was typed a moment ago and stopped, with a message, by a draft refused as stale, moving away twice and back while a save is held in flight with each file keeping what was typed into it, Compare's fork dialog waiting for a draft another page left behind before it copies, an abandon with nothing to abandon writing nothing and answering false, the project form saves after the delay and the page survives the re-read, somebody else's file offers Fork, Fork makes and shows the next version with the check on it, the tree shows one row per author with official and pick beneath and the label as detail, two genres in one save window, the version label, the check marking a pick and moving on a click (official in use wears the star alone; the check on the version in use only says why; the director's star removes their own pick and leaves nothing stale; the star is disabled for a non-director), abandoning a pick and keeping it, a workspace whose owner has a pick writing the tracking value into its own file when it abandons and when the star moves (and writing nothing when the owner's pick already names the new official), the star re-reading the folder even when the pick removal is refused, and the banner a pick the project overtook raises: its two answers, a refusal that writes nothing, nobody else being asked, and the project file asking on its own page; the §13.2 banner on a type with no form of its own and the version it forks, the file menu's editor, folder and clipboard items, the viewer's pick choosing the project version with a project fork picked at once, a rename in the version in use naming the epoch overview, an invalid birthdate never written, and the JSON editor's inline refusals; the epochs row in a provider scope, as in the app, since its boxes' microphones read the dictation settings, and its label keeping more than 150 px of a 239 px card |
| `test/project_menu_test.dart` | the ⋮ menu's items (*Copy to a folder…* offered for a project in memory and not for a folder on disk), the New project dialog's filtering, the mandatory username and the file it announces, the View toolbar's folder and clipboard items on somebody else's file, the library chip |
| `test/phone_folders_test.dart` | a phone's folders through the providers: the ⋮ menu without *Open project…* and *Copy to a folder…*; *New project* without the parent row and the two *Browse* buttons, saying where the project goes, the target and the media proposal under the app's folder, *Create* enabled; *Create* waiting with its reason while the folder is found and refusing with the sentence when it cannot be made; a remembered project under the folder listed by its tail; Settings → About naming the folder and the library card without *Choose folder…* |
| `packages/filmopen_account/test/account_auth_test.dart` | the callback predicate (exact scheme, host, path; `code` or `error` required; fragments refused) and the error mapping to problem codes that never carry the server's text |
| `packages/filmopen_account/test/account_card_model_test.dart` | the sign-in result through the link door: a callback opens the client and is exchanged through it whatever started the app, its refusal a notice and not pending; the same link handed twice is acted on once; another link with a client there is exchanged too; and a second model acts on no link the first already spent, a ledger that forgot being a fresh process (step 2) |
| `packages/filmopen_account/test/account_registration_test.dart` | who registers the sign-in's scheme at run time: the desktops, not a phone, and this machine as its platform |
| `test/account_card_test.dart` | the account card renders signed out without any plugin and without a progress bar as one login button with the logo and no provider name; the button opens the sign-in dialog with the logo, one marked button per provider, the *or* rule and the e-mail box; no microphone on the e-mail box; an invalid e-mail is refused inside the dialog before connecting and its text does not outlive the dialog; Spanish; and every problem code has a distinct sentence in both languages; the card asks the link door as it is built and ignores a link that is no callback; **a callback through the door is exchanged by the card, once per process and not once per card** — a second card replays no spent code, waits for no browser and carries no failure of the first — the Windows argument and the door carrying one link exchanged once, and a door that throws logged rather than thrown (step 2, the stand-in client turns the token ticker off, that periodic timer having been the reason the wiring went untested); the device's notice under the card's first sentence where a sign-in registers, in English and Spanish, and none where none would (§6.6b) |
| `packages/filmopen_account/test/account_profile_test.dart` | the profile service against a loopback PostgREST stand-in with the deployed permissions: load and save under update-only grants, a NULL name reads as empty, a missing row is a `PostgrestException`, a denied write propagates |
| `packages/filmopen_device/test/device_key_test.dart` | the device's key (§6.6b): the website's worked example — the id, the body's hash and the six lines' hash its contract gives, its signature verifying under its key and over nothing else; a key made here — its id the SHA-256 of a 65-byte uncompressed point, canonical spellings, a signature of 86 characters that is the same each time and verifies under its key alone, forty keys from a seeded source all verifying, the stored text giving the same key back and nothing else being one (a character too many, one outside the alphabet, the scalar zero, one above the order, 31 bytes, the spare bits set); `openDeviceKey` — one key made and kept across two launches, the second writing nothing, two calls at once making one, a store that cannot be read making none and writing nothing, one that cannot write giving none, no store no key, a key the store does not give back not used, a value that is not a key written over and said to be, the store's failure never in the exception; `keepDeviceKey` writing a lost key back and leaving a kept key or another key alone; the verifier the tests stand in for the website with taking only what it takes — not a compressed, hybrid or off-curve key, not a signature spelt with its spare bits; a signature with a leading zero byte found and verified; the curve's order refused and one below it taken; the public key read-only |
| `packages/filmopen_device/test/device_registrar_test.dart` | registering the device (§6.6b) against a stand-in for the website's route that checks as the contract does — the path's id, the proof over the six lines, five minutes, after the last accepted: the request byte for byte (method, address, no redirect, the headers, the five fields); four fields without a name; one request for a launch and a sign-in together and for a card rebuilt while one is in flight; a sign-out and a sign-in sending again a second later with the same key; another account dropping the first's request; nothing for an anonymous sign-in, a store in memory, no store or an unknown platform; a store that cannot be read making no key and asked again next time; no token, and a sign-out while the token is asked for; a clock six minutes fast told `stale-proof`, signing again after the site's moment and keeping the offset; `proof-replayed` twice giving up; a refusal remembered and a server's failure forgotten; a clock refusal without the site's moment, and an answer that is not JSON; every refusal of the contract — nineteen codes over `400`, `401`, `403`, `405`, `409` and `413` — logged by its code, sent once and remembered, a redirect not followed and not an answer, and `500` and the two `503`s asked again; a clock refusal signed again after a moment the site took, not before it; a token that never comes given up on in time with the next account not held behind it; an anonymous sign-in dropping the request of whoever was signed in before; no network and no answer in time, by their types, tried again; a key lost while the request was out written back; the log holding no name, token, key, proof or server's words |
| `packages/filmopen_device/test/device_name_test.dart` | the name a device suggests (§6.6b): a host name trimmed; control characters, the two separators and lone surrogates dropped, nothing left no name; 80 code points, an emoji counting once |
| `packages/filmopen_account/test/account_device_test.dart` | the account telling the device's registration (§6.6b) through a stand-in seam and a stand-in sign-in service: a sign-in names the account, not anonymous, with a token that is the session's; a sign-out says so and the token is then none; an anonymous sign-in says it is; a session past its hour refreshed before its token is given; a card that finds no session telling the seam nobody is signed in; a launch with a session telling the seam with no card (`announceRestoredSession`), and one without a session or failing telling nothing and throwing nothing; a model that never connects says nothing |
| `packages/filmopen_state/test/seams_test.dart` | the device registration seam a no-op until one is installed (§7.1); the exit tasks run together and all waited for, one that fails, before its first await or after, keeping none waiting and one taken back not running; the seams of §7.1: the no-op host installed by default and answering "not available" to everything, `install` replacing it and leaving `windowKey` and `rootKey` two distinct keys, the field registry routing to the form that registered and forgetting one that left, the draft observers hearing each change with its key **and value** until removed and unable to write into the value they hear or into what another observer hears; the four doors step 2 added (`openProject`, `setViewer`, `setLocale`, `setTheme`) answering nothing and changing no setting on the no-op host; the route seam silent with no listener, naming a dialog's push and pop by its name and a page's push, and a failing listener never stopping a dialog from opening |
| `packages/filmopen_plugins/test/plugin_manifest_test.dart` | the manifest (§6.11): the sample library's 1.10 manifest and the template read with no problem; an unknown field, entity hook, platform hook, setting type and action reported with the rest kept; a mistyped field inside a setting, a key slot and an action reported with the entry kept, a field the setting's type does not use (`values` on a text box) reported, and bounds no number meets left out; a wrong type in every field reported, never thrown; an issue's values part of its equality; `api` 2 refused naming both versions; another type, a bad tag, the tag `app` and a missing or mistyped header field refused; the code file named after the stem, and one elsewhere refused; key slots named `<tag>:<slot>` with their store and preference names, one naming a platform without a file reported and kept, a bad slot left out; `uses` with the app's key, malformed ids reported and repeats dropped; `KeyId` reading only two segments; a default of the wrong type dropped, an enum without values and a model without a category left out; scope on settings and none on entity settings; entity settings for a type not applied; network hosts |
| `packages/filmopen_plugins/test/plugin_strings_test.dart` | a plug-in's strings (§6.11): metadata skipped, a key with a colon and a value that is not text refused, `@@locale` checked against the file's name, a file that is not an object or repeats a key; `pt_BR` reaching `pt-BR`; the template's two languages holding the same keys; the fall-back from locale to language to English to the key, reported once; the plug-in's own namespace, `app:`, and another plug-in's; an app string never picked up by a missing key; placeholders; a manifest's name as a key or as written |
| `packages/filmopen_plugins/test/plugin_package_test.dart` | a plug-in folder and its zip (§6.11): the template in a library read with both locales; a stock manifest and a fork in one folder; a folder without a manifest, a manifest of another tag or stem, and one that does not parse, reported; a platform file for a new id making a slot's platform known and one for a catalogue id refused; a file that is not text reported by its path with the rest read, a manifest's among them; a missing code file and a folder without English reported; locale files under the folder a manifest names; the template zipped and read back, nested or flat; an entry escaping the folder — `..`, absolute, a backslash, a drive letter, `.`, an empty segment — or a link refusing the whole archive; macOS's `__MACOSX/` and `.DS_Store` left out; an entry inflating past the limit refused, honest or lying about its size; a damaged entry refused as unreadable; a folder or manifests naming another tag, or no manifest; bytes that are not a zip; the default AI assistant read with no problem, its two key slots and `capabilities.ai`, in both locales with the same keys |
| `packages/filmopen_js/test/engine_test.dart` | the engine (§6.11), each call in its own isolate: a function called with JSON and its value back; a promise awaited and two Dart doors awaited from JavaScript; a door's refusal reaching the script with its code and reason, and another error with no text; an action called with `ctx` alone, with its settings, tag and strings; `eval`, `Function`, `Math.random`, `Date`, the host's own functions, `require` and `fetch` absent and every function's `constructor` refused; an infinite loop inside `try` ending at the time limit while this isolate's timer kept ticking, a loop at the top of the file too, waits on a door and on `sleep` not counted, a growing array ending at the memory limit though caught, deep recursion at the stack limit with the default 1 MB, a result over its limit, the wall clock, and a sleep past it ended by it rather than answered as slept even while the caller is kept busy; a script's thrown message, a syntax error, a missing function, a value JSON cannot hold, a promise nothing settles; a looping, a waiting and a sleeping script cancelled at once; flooding the doors ending at the memory limit without the process growing by gigabytes, and so does a slow door holding what was drained to it; a prototype's `toJSON` and a thrown `cancelled`, `hung`, `limit`, `engine` or `stalled` code faking nothing, a door's `refused` kept; a stack trace's call sites not handing the script the host; replacing `JSON`, `Reflect.apply` changing neither the doors reached nor the outcome; a cancel before the isolate started; a last `log` and a missing string still reaching their doors, and requests queued before a time limit other than a `log` not started; a cut message, a lone surrogate and a function as a request's arguments read back; the result limit in bytes; a throwing microtask not failing the call; and a thousand calls growing the process by less than 100 MB |
| `packages/filmopen_plugins/test/stock_install_test.dart` | the stock install (§6.11): an empty library getting every file and a record, a second start changing nothing; a file at the top of the bundle — the interface definition — belonging to no plug-in and installed nowhere; an update replacing its own file while a person's fork, their locale file and their own plug-in stay; a file of the same name the app never wrote left alone; a file the bundle dropped removed unless the person changed it; a deleted stock folder back at the next start, and gone at every start while the person keeps that plug-in disabled |
| `packages/filmopen_plugin_host/test/host_runtime_test.dart` | the host against stand-ins (§6.11): `ctx.http` sending the platform file's header filled from the store, with nothing the plug-in reads holding the key though the platform echoes it and sets a cookie; a host off the platform's, a redirect elsewhere, `http`, a key neither owned nor granted, a key with no platform file and an unlisted free host refused with their reasons; a granted key sent and, its grant taken back, refused; a key echoed JSON-escaped still replaced, and in a text answer JSON-escaped, percent-encoded or as character references; six asks at once reaching the provider one at a time; a nested call refused another; a provider of `complete` refused another provider's key without `capabilities.ai`; a caller cancelled or answering without waiting ending its nested call, with no request after it and every usage chain ended; a hung plug-in off until the next start; `ctx.ai.complete` from the text provider itself running, recorded as the action the person ran with the entry and model id, and refused to a plug-in without `capabilities.ai`; a key sent from an action recorded; the default AI assistant against stand-in OpenRouter and fal — *Ask* with the film's text model and the cost its nested `complete` reported, *Ask* without a key refused, `render` through fal's queue to a take with its thumbnail, `estimateCost`, its folder deleted and put back at a start unless turned off; a disabled owner's key refused and a key checked by nobody but its owner; a transform cancelled midway leaving no version and no batch; a transform refused on a type it does not apply to and its batch without the machine setting; a film's folder under a stock tag not taking its place; `verifyKey` reaching the candidate for one call with nothing stored; `ctx.project` answering the viewer's versions with their `plugins` object; the template's `transform` written as `<viewer>.template`'s v1 with its batch and the person's version untouched; a `render` against a stand-in fal's queue downloaded without credentials as a take by `<viewer>.<tag>` with its thumbnail and a plugin batch, and its usage chain `started` then `succeeded` with the stem, the entry, the platform, the key's last four and the reported cost; a loop at the time limit and a throw with its message, the next call running; a third party's plug-in off until allowed and off again when its code changes; a stock plug-in trusted and granted with the index read again after the install, and a fork beside it asking; Provider keys' sections; and, since step 7, a key check that may only read with the key it checks, a `POST` refused `keyCheck` with nothing sent; `verifyKey`'s answer held, `{ accepted: "yes" }` being `badResult` and no refused key; an answer's header names never carrying the key back; and a `transform` answering an entity without its name `badResult` at `[0].name`, nothing written; and a call that waited for one of the five slots, its plug-in turned off meanwhile, `disabled` and never run |
| `packages/filmopen_state/test/legacy_keys_test.dart` | 4.9's key names moved once to `filmopen/default-ai-assist/…` and `filmopen/app/openai`, a key already under a new name kept, the account's session untouched, a second call opening nothing; a store that cannot be used leaving the move for next time; the preferences' states moved without opening the store (§6.7) |
| `packages/filmopen_l10n/test/app_strings_by_key_test.dart` | the generated `appStringsByKey` equal to the ARB files as they are (`scripts/gen_app_strings.py`) |
| `packages/filmopen_plugins/test/plugin_archive_memory_test.dart` | alone in its file, so that the process's high-water mark is its own: a gigabyte of zeros behind a zip whose directory claims one byte refused as too large with the peak memory growing by less than 400 MB (§6.11) |
| `packages/filmopen_l10n/test/arb_keys_test.dart` | every key of `app_en.arb` present in every other language file, and no key in another language that English lacks |
| `packages/filmopen_automation/test/protocol_test.dart` | the agent data channel (§4.3) on an injected host: `ping` as in step 1 — literal, empty, missing or JSON — whatever the host; every op reaching the host with its arguments and answering what the host answers, one case per op; a host that cannot act, the product's no-op host among them, answering `not_available` to every op and hearing nothing, and a host without `Recording` answering `not_available` to `record_start`, `record_stop` and `record_state` alone; a message that is not an op refused as `bad_args` whatever the host, and every malformed argument refused as `bad_args` naming the part before the host hears of it, a language the app lacks and an empty label among them — on a host that can act, since one that cannot answers `not_available` first; an unknown op refused, and named only when it is a name; `not_found` naming the identifier, the field or the kind of target, never the typed text or the label; a host without the semantics taps answering `not_available` to a tap by label or position, and one that cannot exit to `quit`; `tap_at` marked a diagnostic; the log travelling without an error's message or stack, nor the data of the account's events, and a line that is not an event staying behind; a host's refusal travelling as its code, and an unexpected failure as its type, logged; ops one at a time in arrival order, with neither a ping nor an unknown op waiting; an op past its time answered `timeout` / `running` and still ending before the next starts, one whose time ran out in the queue answered `queued` and never run, and a late success, refusal and failure logged; a selection of every kind in the plan's shape and back; `find` over a semantics tree; `set_tab` naming Settings' `general` and `keys`; the `usage` tab; a selection of the kind `script` and of the type `fo` surviving the trip, and the types `se`, `ep` and `sq` refused as bad arguments |
| `packages/filmopen_state/test/semantics_walk_test.dart` | the walk over the semantics tree (§4.3, §7.1, §6.8), on a pumped tree:
 - a node by its identifier, and by its label (a tab by its label's first line);
 - the tabs of a tab bar;
 - a tap going through a wrapper to its button, and from an identifier inside a tappable item to the item, but never through a disabled button, nor up past a disabled control, a button without a tap or a route;
 - a pull-down (`DropdownMenu`), whose field offers `expand` rather than `tap`, opened through its identifier and an item chosen by its label, and a disabled one refused although it still wires `expand` (`activationOf`);
 - a rect in logical pixels at a pixel ratio of 2;
 - the tree as data: a project field's value travels, an unidentified text field's value is withheld, an e-mail address is masked in a label, and a provider key's obscured box travels neither its value nor its bullets, while a key box shown by its eye is withheld by its identifier, not by obscurity;
 - the entity page's tabs by their ids and the selected one, in English and Spanish;
 - the control at a point (`semanticsNodeAt`, `describeControl`, the issue recorder's rule): a button by the identifier around it, and the same by its node id; a chip by its label within the row that carries the identifier; a pull-down by its identifier opened with `expand`, and its item by its label once the pull-down is open; a dialog's button by its label, borrowing no identifier from beneath the navigator; a tab as a tab; a point on nothing that takes a click not resolved; a control only a tooltip names, and a second control of the same label, not resolved; a control keeping its own identifier though it holds another control (a row around a star button, a text field around a clear button), one drawn outside its parent still found, and a masked label not resolved;
 - `textValues` and `focusedTextIdentifier`: a value read under `new-entity.tag` and withheld under `account.login`, and the focused box named only under a project identifier |
| `packages/filmopen_automation/test/ring_log_sink_test.dart` | the host's copy of the log (§4.3): an event's line with its time, level, name and data, and an error by its type only, never by its message or stack; the ring keeping the last lines, oldest first; a value JSON cannot hold, written as its text; the log's threshold still applying |
| `packages/filmopen_recorder/test/tape_test.dart` | the tape on its own (§6.8): one JSON object per line, LF, `seq` and `t` rising, `at` in UTC milliseconds; every text masked, in keys, lists and maps, with a typed value outside a project identifier refused; a report path refused when it could leave the folder; the log's tail keeping this session only, from the app's last start; a value JSON cannot hold written as its text, masked |
| `packages/filmopen_recorder/test/snapshot_test.dart` | the project snapshot (§6.8): every text file copied and media left out, the copy taken after a write differing from the one before by that write; the folder on disk appending the tape line by line and writing files beneath it; a recording's folder named after its second, and a second one in the same second getting `-2` |
| `test/recorder_test.dart` | the issue recorder on the whole app (§6.8): a dialog is in `windowKey`'s picture and not in `rootKey`'s, and the route seam hears it open and close; a scripted sequence — a character's box, the *Low* chip, *Add character* with a tag typed and cancelled, Compare, Settings and the handle — on the tape in order, with a picture and the state after each action, no account value, the project's files before and after, a tape whole line by line before `stop`, and every door given back; a door that fails mid-recording leaves a readable tape and the recording goes on; a start that fails gives back every door, logs its type and names no folder; the rail's ? opens *Get Support* and *About* (the specification's version, the open-source notices), its *Record an issue* closes the dialog and starts a recording, and the ? becomes the stop, which opens no dialog; the dialog fits at 375 and 1200 px in Spanish and dark, on both tabs |
| `test/agent_host_test.dart` | the agent's host on the whole app (§4.3), on a fixture. **Setup and navigation:** installed and attached, it names the project; it navigates to a version and refuses a selection that names nothing. **Compare and typing:** it opens Compare, copies a row by its identifier and types through a form's own setter; the autosave writes both, and the log names `compare.copied` and `entity.saved`; a pull-down takes no text; an unknown identifier and a tab the page lacks are not found; a blocked Compare is `refused` with its reason's code. **Fork and dialogs:** `find` names the version row's four controls; a fork by its button lands as a click's does, the state naming the new stem, the tree its row and the log `entity.forked`; Compare's question is `intercepted` and answered by a dialog button's label. **Page and settings:** the page is painted as a PNG; Spanish and dark go through Settings' doors, a language the app lacks is refused, and in Spanish the tabs still answer to their ids; an e-mail address typed as the handle browses as nobody and travels in the state only masked; a tap at a position opens Settings. **Recording:** the recorder's ops (`record_start`, `record_stop`, `record_state`) starting, naming and stopping a recording, and its tape hearing the agent's taps marked `via: semantics`. **Quit:** the open drafts written before the answer, the exit asked for only after it, and a running recording stopped first, its tape ending with the exit; a draft that cannot be written refusing the quit, and the application staying; the settings the session started with put back through Settings' doors. **Unattached:** nothing is answered before `attach`, and the host is not available after `detach`; Settings' tabs by their ids; `quit` putting dictation's microphone and switch back too |
| `packages/filmopen_mcp/test/server_test.dart` (`launch` on **android** requires a device and refuses a size, a home or a device on a desktop, the device reaching the launcher, and a client that sends `"size": null` is refused by the tool's own schema, which names the property, before any argument handling is reached) |
| `packages/filmopen_mcp/test/server_test.dart` | the MCP server (§4.3) through an MCP client on an in-memory channel, against a fake link:
 - **Tools:** a tool for every op of the app's channel, `quit` among them (checked against `filmopen_automation`'s `agentOps`), beside the window, the wait, `enter_text`, `launch` and `stop`; `enter_text` handing its text to the link's text entry and never to the channel, answering `typed`, and refusing a missing or non-string text as `bad_args`.
 - **Answers:** each op tool sending its op with its arguments and never the URL, and answering the result as JSON text; `page_shot` and `screenshot` answering image content; an app's refusal returned as an error result with its code.
 - **The link:** a failure on the link, or a request past its time, dropping it, answered by its type without the URL, and reconnected on the next call; a `vmServiceUrl` that is not a string, or not on this machine, refused as `bad_args` for `state` and for `stop`, the link kept; the announced agent build found by itself, looked for again only once its link fails, and `no_app` without one; a service that is not an agent build named so, and nothing kept; a window shot written only as a new `.png` inside its folder, or over one the server wrote, and any other path — outside it, not a PNG, another's file, one the file system refuses — answered `bad_args`, the link kept; `wait_idle`; calls sent at once opening one link between them; calls still queued when the client leaves answered without running, no launch starting, and a link opened meanwhile closed; a client leaving asking the app it launched to `quit` before `q`.
 - **Launch and stop:** `launch` starting, linking, waiting, applying its options and answering the state, and refusing a second launch and a platform it cannot run; the launched app outranking the URL named at start; a launch whose app does not let the driver in answering `launched: true`, and still stopped; a launch that fails before its app answers giving its reason, its exit code and its last lines; a launched app that ends by itself forgotten with its link, its launcher asked once to clear what it left; a client leaving mid-launch stopping what was started; `stop` ending a launched app with its `quit` and `q`, over a new link when none is open, or saying it had to kill it; a draft it cannot write keeping it running, a quit still writing waited for (`timeout` when it stays), and an app without `quit` ended with `q` (`by: q`); a `stop` that names another app ending that one, the launched one staying; any other app ended through its own `quit` and waited for (`timeout` when it stays), a refusal to quit passed on, and `no_app` with nothing to stop. |
| `packages/filmopen_mcp/test/discovery_test.dart` | finding an agent build (§4.3): the log folder on each system, as the app computes it; an announcement of a service on this machine read, and anything else — another machine's included — refused; a build found while its service answers, and not once it has gone, nor from a half-written file or no file; a stand-in service answering, and a closed port not |
| `packages/filmopen_mcp/test/launcher_test.dart` (an **android** command line takes the device's serial where a platform's name would stand and never `xvfb-run`, a launch with no device is an `ArgumentError` rather than a guess at `-d android`, a launch on a Linux host gets a process group of its own whatever the target, the keys reach an android build, and android's launch environment is empty, the screen being the device's) |
| `packages/filmopen_mcp/test/launcher_test.dart` | starting an agent build (§4.3): every launch passing `FILMOPEN_BUILT_AT`, the moment in UTC to the second, last (§6.6a); the service line `flutter run` prints matched, and the DevTools line not; the command for Windows, and for Linux under `setsid xvfb-run` with the Xvfb screen of `size`; `parseWindowSize`; the launch environment (`FILMOPEN_WINDOW`, the XDG folders on Linux); `prepareAgentHome`; on Linux a process group ended as a group, and a launch that makes a temp XDG home which `stop` removes, a named home left; the repository an executable belongs to; against stand-ins for `flutter`, a launch answering the printed URL, a second launch refused, `q` reaching the process and ending it, a process that ignores `q` ended once its time runs out, a stop asked for while `flutter run` is still being started ending it once it has, and a `flutter run` that ends first failing with its exit code and its last lines, the e-mail address and the token cut out |
| `packages/filmopen_mcp/test/cli_parse_test.dart` (`launch android --device <serial>` parsed, a size or a home there a usage error, a device on a desktop a usage error, and **the command line built from the command as parsed** — the join where `--device` was once parsed and then dropped with every suite still green) |
| `packages/filmopen_mcp/test/cli_parse_test.dart` | the executable's command line (§4.3), read as the executable reads it: no words, and `serve`, serving MCP; every op command's message, `quit`'s included, and words starting with a dash (`tap --label`, `open-project --sample`, a negative number); the executable's own options anywhere before a `--`, the words after it the command's, and `--help` wherever it stands; pictures written to a named or default file; `launch` on this machine's platform unless it names one, with `--size` and `--home`; `enter-text` with its one word; `record-start`, `record-stop` and `record-state`; `stop` and `wait-idle`; a command line it does not understand, an option without its value, a `--size` outside `<width>x<height>` 320–4096, an empty `--home` or an `enter-text` without its text, refused with a reason |
| `packages/filmopen_mcp/test/app_link_test.dart` | the link to an app (§4.3), against stand-ins on the loopback: the token cut from every service URL in a text, with or without the slash after it, and percent-encoded; a service URL on this machine being `http` on the loopback; the WebSocket address the driver derives, from the URL's first path segment; a service that answers probed and let go; a closed port refused at once, and a listener that never answers refused once its time has passed, as `ServiceUnreachable` naming no address, to the probe and to a connection; the wait for a service to end; a VM service whose isolate carries no driver extension refused as `NotAnAgentBuild`, its connection closed; the time running out in a request `NotAnAgentBuild` where the service had answered and `ServiceUnreachable` where it never did, never a bare timeout; one whose isolate carries it sent the driver's own commands, a request past its time failing; and one whose isolate is paused at its start resumed, then connected |
| `test/ui_cleanup_test.dart` | the header with the name alone, no *Version* word and nothing after Fork, the label button between the pull-down and the star; the file menu on the first card's title row without making it taller, and no save status while saving; closing the window writing a draft typed inside the save delay, and ending an open dictation, before answering *exit*; Compare's left heading without pencil, check or status, and `<<` and `<<<` centred on the rows' gutter; the epochs without chips or explanation; *Close project* and its blank page, the menu then offering neither *Close project* nor *Re-read files*; **×** taking a remembered folder off the list with the folder kept, the trash's *No* doing nothing and *Yes* deleting a real temp folder and its row; *Edit JSON…* above *Copy JSON*, opening on the tree, a tapped value changed, applied and saved; a long value kept to its row at 700 and 1200 px; the microphone on the value dialog and never on the tree or the text mode |
| `packages/filmopen_forms/test/json_tree_edit_test.dart` | the JSON editor's tree without a widget: every path the tree spells reaching its value through objects and lists, a value written at exactly that path, a path two keys would spell alike left out, and a typed value keeping its kind |
| `test/new_entity_test.dart` | *Add character* and *Add location*: the dialog's identifiers, a taken tag refused inline with *Create* disabled, the short name filtered and the file it will write named, the name, kind and author written and the page moved to the new version on Edit; the epoch pull-down only where the project has several, an empty name written as the short name and no kind written when none is chosen, at 700 px without an overflow; a file of that name appearing before *Create* refused as taken with nothing replaced and the handle put back; the username typed in the dialog written as and saved to Settings; the button disabled with the reason on a read-only project; a disabled *Create* saying why and naming the box — the short name's problem, a taken one, then *Type your username first* on the button's own semantics node, and nothing once it is enabled |
| `test/identifiers_test.dart` | the semantics identifiers an agent acts on: unique on the overview, a version page, the open project menu, the New project dialog, a category page and the New location dialog, the label dialog, Compare and Settings; the rail (`nav.help` among them), the tree pane and its rows, the project menu, the version row, the file menu, the fields (file and key), the dialogs and the copy controls named; a form registering its setter, typing through the registry reaching the box by the field's own setter, a pull-down and a key no field owns answering false, the locked column having no setter; Settings' Provider keys tab; the microphones on the tree's filter, a form field, both dialogs and Settings' handle, the label dialog's box, the popup's doors opened from a filled box and from a blank one, and the Usage tab's; none on the New project dialog's folder box or the Provider keys boxes |
| `packages/filmopen_format/test/platform_file_test.dart` | the catalogue's platform files (Project Specification §14.6, §14.9): the bundled index's sixteen platforms, the first version's three first, then xAI, dictation's second speech service, the four supported and every other pending with its obstacles; every listed file parsing with no issue under the id it is listed with, and the supported ones carrying what Provider keys needs; a check's `rejected` as its whole list, 401 and 403 where it writes none, a list that does not read leaving the check out, and xAI's bundled check counting 400; a file that cannot be used reported by code and never thrown (not JSON, a missing or mistyped field, an unknown kind, a key platform without credentials or with a placeholder no credential fills, a credential id that is not a plain token or repeats one, a header that is not `Name: value`, a keychain other than `filmopen/<id>`, a base URL over http or off its hosts, another id), a bad page URL leaving the file usable, and a check that cannot be used (a method that is not a token, an http or unlisted URL) leaving the check out and the file usable; credentials only over https or wss to a listed host; **`paths`** — prefixes matched by whole segments, a host it does not name taking any path, a path that cannot be made plain refused `badPath` before any match (read raw where the caller has the text, and as sent where it does not; an escaped `%` among them), a `paths` naming a host `hosts` does not, a prefix that is not plain, a value of the wrong shape, an empty `paths` and a host named twice each making the file unusable, a `verify.url` outside them leaving the check out, and **every bundled file's `verify.url` and every access row's `endpoint` passing its own file's `paths`**, read from the catalogue; the header filled from both fields of a two-field platform; an index status other than `pending` read as pending, and a listing without its file, name or kind, or with one of the wrong type, left out |
| `packages/filmopen_platform/test/credential_store_test.dart` | the credential store's seam (§6.7): nothing installed failing closed; the memory store round-tripping a value, counting every call and failing when told; the device store running one call at a time, in order, and turning an operating-system failure into `CredentialStoreUnavailable` with its type only, the next call still running; a call the operating system never answers failing closed as `timeout`, the queue moving on |
| `packages/filmopen_platform/test/key_verifier_test.dart` | the key verifier's seam (§6.7): nothing installed checking nothing and sending nothing; the stub answering by how a fake value ends |
| `packages/filmopen_keys/test/http_key_verifier_test.dart` | the check's request against `MockClient` (§6.7): a 2xx verified, with one request to exactly the check URL carrying the key in the platform's header and its other headers; 401 and 403 rejected, and any other status not checked; a platform's own refusals, the statuses its file lists and those alone, with no body read, and the bundled files — a wrong key rejected on xAI, which answers it 400, where OpenAI's 400 still could not check; a 302 not followed; a timeout and a failed connection; the body never read; the method sent as written, and a value dart:io refuses in a header answered as `bad_value`; a check on an unlisted host kept out of its file, a `wss` check refused, and nothing sent without a value |
| `packages/filmopen_keys/test/provider_keys_test.dart` | the keys' controller on the memory store and the stub (§6.7): *Save* checking a value with the platform and storing one trimmed JSON object under its slot's store name, `filmopen/<owner>/<slot>` where the platform accepts it, and replacing a stored key; a refused key and an unanswered check, neither of which reaches the store, both keeping their code for the line under the box; a platform with no check, stored unchecked; *Remove* deleting that name alone; a two-field platform stored as one entry; an unusable store changing nothing, without throwing; the states in memory for an ephemeral store and in the preferences otherwise, read back without opening the store; log events carrying the platform and a code only; no last four for a short value; one action per platform at a time, recorded for the cards, another platform not waiting; a verifier that throws, which could not check; a value cleaned of invisible characters, and one no key could hold refused; a refusal keeping its code; a preference that is not text, and a state the preferences refuse to keep; a state read from JSON; a platform with no `verify` checked by the owning plug-in's `verifyKey` — accepted and stored, refused and not stored, a failed check not stored with its code |
| `packages/filmopen_plugins_ui/test/plugins_tab_test.dart` | Settings → Plugins over a runtime the test answers (§9.4): the web build's one sentence; a plug-in that needs consent turned on only through *Allow*, *Cancel* leaving it off, the consent naming its keys and hosts, a 44 px *Allow*, turning off asking nothing; a plug-in's page — a machine setting saved 700 ms after typing and through `FieldRegistry`, a number box saying inline that its text is not a number and saving nothing, an action running once at a time with its answer, a key not entered said as such, a grant; the preferred providers' pull-down and its line when none provides, *None: ask me* shown where none is chosen — never the first plug-in as if chosen — and chosen to take a choice back, and the line saying when the preference is asked; the list, the consent and the page at 700 and 375 px in English light and Spanish dark |
| `packages/filmopen_keys/test/provider_catalogue_test.dart` | the tab's platforms out of the one reader's catalogue (§6.7): a platform whose card would repeat the tab's identifiers left out and logged, pending ones counted; a catalogue that could not be read gives the tab nothing, and the tab logs nothing of it |
| `packages/filmopen_l10n/test/labels_plugins_test.dart` | a plug-in call that came to nothing, in the person's words (§6.11), English and Spanish: no model, no entry, nothing on and able to run serving it (with the platform where one was named), `ctx.ai.complete` with no model and no text plug-in, several serving a model or several text plug-ins with nobody's choice, a render still pending, an output that is not its `mime`, and a call on a plug-in not installed, off, on and unable to run, or offering no such function; *Settings → Plugins* built from the tab names; a reason with no sentence null; and a plug-in's problem in its sentence (5-2's) — `badCodeFile` in its four cases and `archiveTooLarge` with the folder's two limits from its `args`, `64 MB` and 2000 files, in both languages and never naming the code, a zip's refusal without limits and a code with no sentence null — and, beside them, that the write refused because the film is gone is a sentence and never the code, in both languages and by its own arm |
| `packages/filmopen_l10n/test/labels_keys_test.dart` | a key check's codes to their sentences, a refusal's sentence for a 403 and for any other, and the key boxes' labels translated where FilmOpen knows the word, the platform file's own otherwise |
| `packages/filmopen_l10n/test/labels_dictation_test.dart` | the Usage tab's names for the boxes a use was for, a project's name as its *Title*, and its failure sentences without the popup's *Resume*; a sentence of its own for every code a dictation gives, `canceled` included, in both languages, the two refused-key statuses sharing one, and an unknown code naming itself in the general sentence |
| `packages/filmopen_keys/test/provider_keys_tab_test.dart` | Settings → Provider keys on its own, over the bundled catalogue (§9.4): the explanation, then a card another package brings (`leading`) between it and the keys and none on the web build, then FilmOpen's OpenAI and xAI and the Default AI assistant's OpenRouter and fal, four boxes in all, xAI's card with its *How to get a key* as the others, then the pending line, every identifier once and no store opened; obscured boxes without suggestions, every disabled action's reason, and *Save* ready as soon as every box is filled; a mismatched prefix warned about only once what was typed can no longer become it, and saved after *Save* asks, the box emptied, and the key on no widget and in no log line; a refusal and an unanswered check, each with what to do, nothing stored and the box keeping the paste, and then the same button storing it once the platform answers; a platform FilmOpen cannot check, stored unchecked; *Remove* asking first, with a 44 px confirm; an unusable store said inline, the box keeping the key; the web build's sentence; an unreadable catalogue; the links' addresses, and a link that cannot be opened; through `HttpKeyVerifier` on `MockClient`, a 200, a 401 and a timeout, with the checking line and every action's reason while it waits, and each request to its own platform's check address; no way to show a key; a card built again while its key is on its way to the store, *Remove*'s question outliving its card, no undo bringing a key back, *Save* asking first and its cancel keeping the box, Enter saving, a value no key could hold, and a 403's line; the longest lines, none cut, at 635 and 295 px, light and dark, English and Spanish; the eye on a secret box: shows the key, hides it, a refused key stays shown, a stored one is not left on screen, and a box that is not secret has no eye; while a recording runs every box is hidden and the eye says so, the choice standing once it ends; the busy eye says why |
| `packages/filmopen_keys/test/usage_tab_test.dart` | Settings → Usage on its own (§6.7, §9.4): the web build's sentence; an empty log; the totals by month and platform, newest first, a live session counted as spent and a waiting estimate as pending, a refused call counting nothing; each use with what and which box, when, the minutes, its state, a failed dictation's sentence and its cost at the line's end, newest first; the month filter; Spanish decimal marks and currency; a call of a version this app does not know left out and said; an append showing at once, and another copy's record on reopening; the file each box belonged to; the words *pending* and *possibly charged* explained where they are shown, and only there; nothing cut or overflowing at 635 and 295 px, light and dark, English and Spanish |
| `packages/filmopen_keys/test/usage_providers_test.dart` | what the Usage tab reads (§6.7): the log read, and read again after this app appends a record; calls newest first, by the month they count in, and the totals as §6.7 says; the month filter starting on every month and keeping what it is given; no log installed reading nothing, without throwing |
| `test/settings_tabs_test.dart` | Settings' three tabs on the whole app (§9.4): *General* first, *Provider keys* over the bundled catalogue without opening the store, and back; a tab asked for from elsewhere; Settings opening on *General* again after it was left on *Provider keys*, even when a tapped tab was still moving; the first scrollable under Settings still General's list; both tabs at 700 px, light and dark, English and Spanish, and the tab labels at 375 px; *Usage* reading this device's log, opened by tap, and fitting at 700 px |
| `packages/filmopen_format/test/money_test.dart` | money as exact decimals (§6.7): a JSON number read exactly from its text, exponents included, and nothing else taken for one; amounts rounded half away from zero to twelve places; a cost at the catalogue's price times its units; a reported cost below zero or with more than twelve places being no provider cost; a money object with a sign, an exponent, other than twelve places, another scale or no basis being none |
| `packages/filmopen_format/test/catalogue_entry_test.dart` | the catalogue entry reader (Project Specification §14.7): an entry's id, kind and access rows with their prices; two prices for one unit, or two rows on one platform, read as no answer rather than a guess; an entry that is not JSON, not an object, names a field twice or lacks its id, kind or access, not read; the bundled live-transcription entry read at OpenAI |
| `packages/filmopen_format/test/catalogue_test.dart` | the one catalogue reader (§6.7, §6.11): the listings, every platform file pending or not, and each entry once in the index's order; what cannot be read or used a problem with a code and never a message; an unreadable index an empty catalogue with the one problem; an index that is not an object said so |
| `packages/filmopen_format/test/usage_record_test.dart` | the usage record of §6.7, version 1: a started record; a later record naming the first in `ref` and repeating its fields, while `v`, `id`, `at`, `status`, `ref`, `app` and `error` are its own; a record version 1 does not allow refused as the app builds it; a record's time kept after the chain's latest, even under a clock that does not move; ids as random version 4 UUIDs in lower case; a line that is not a complete record skipped with its reason, and fields a reader does not know kept; a record of a version this reader does not know naming its chain, which is skipped whole; a record's JSON read back as written |
| `packages/filmopen_format/test/usage_totals_test.dart` | the chains and totals of §6.7: each chain counted once, by its counted record, as its status says; the counted record the one with the latest `at`, and on a tie the greater `id`; a correction carrying the counted record's status never reopening a call; records whose first record is missing counted as one chain, in the month of the earliest; a chain counted in the month of its first record; a chain holding a record of an unknown version left out whole; months newest first, then platforms and currencies in order |
| `packages/filmopen_store/test/usage_log_test.dart` | the usage log on disk (§6.7): two processes appending under the lock, leaving 400 distinct records, no broken line and one device id; a write waiting for a lock another process holds, and giving up after its time with `lock_timeout`; a last line cut short getting a line feed before the next record, and skipped and reported once; a record of an unknown version leaving its whole chain out, reported once; `device.json` made once, its id kept by every log of the folder, and made again, through a rename, only when it holds no valid one; a lock file that cannot be opened failing at once as `io`; a folder with no usage yet reading as none; a later record retried until it is written, and a log unavailable altogether giving up; no log in use until an entrypoint installs one |
| `packages/filmopen_dictation/test/transcriber_io_test.dart` | the device's WebSocket against servers of the test's own on the loopback (§9.6): a handshake answered with a redirect not followed, ending as its status, the key reaching only the server asked; an upgraded handshake carrying messages both ways; a server that is not there being a connection that did not open |
| `packages/filmopen_dictation/test/openai_transcriber_test.dart` | a close under way ending the session without a code, even when the connection then fails or the time for accepting runs out; OpenAI's live transcription over a `StreamChannelController` (§9.6): the session message exactly D8's with turn detection off, pinned whole; a commit and a clear sent only while the session is accepted and open; a turn taken, a turn that could not be transcribed and an empty commit refused as the session's events, and other errors after acceptance ignored; a session turned down carrying the platform's type, code and field, each only where it is a code or a field path; text by turn, a completion replacing its own turn and no other; a refused handshake, a connection that never opens and a session turned down, each ending before it opened; a connection that drops after it opened ending the session with its minutes counted; a session never accepted giving up after its time; no transcriber until an entrypoint installs one; the scripted transcriber answering frames with turns, and a commit with the turn in progress, or with a refusal where there is none |
| `packages/filmopen_dictation/test/xai_transcriber_test.dart` | xAI's live transcription over a `StreamChannelController` (§9.6): the session asked for in the URL and the key only in the header; raw frames; text sent whole, a locked chunk in front of words that can change, and an utterance's end under both readings of xAI's pages, neither repeating nor losing words; a wait of its own for each commit, two at once, a `finalize` left unanswered and the platform's own end; audio in flight when an end arrives; refusals, time-outs and drops, the platform's message never travelling |
| `packages/filmopen_dictation/test/xai_settle_test.dart` | the real `DictationController` on the real `XaiTranscriber` and a platform of the test's own: a pause, then more words and *Apply* before the first answer is back, and `settle` waits for both turns' words |
| `packages/filmopen_dictation/test/dictation_engine_test.dart` | the speech services (§9.6): xAI's setup out of the bundled catalogue, refused as OpenAI's is; a slot of the app's for every engine's platform; the device's transcriber telling an engine by its endpoint's host; the choice Settings keeps, the default where it names none or an engine this build lacks, deciding the setup |
| `packages/filmopen_dictation/test/dictation_engine_card_test.dart` | the *Speech service* card: each service by platform, model and price, the words as read, in both languages and when the language changes under it; choosing one; 375, 532 and 700 px, light and dark; nothing drawn where the catalogue serves one; the popup on xAI reading xAI's key, naming no platform, and its records naming xAI's platform, entry, model and price |
| `packages/filmopen_dictation/test/speech_gate_test.dart` | turns decided on the device (§9.6): a frame's level as the root of its samples' mean square; quiet committing nothing and cleared every ten seconds; speech, then 0.8 s of quiet, committed once; a click cleared and a two-frame name kept; a pause under 0.8 s keeping the turn; talk without a pause committed at 30 s and going on in a new turn; speech already under way when the gate starts heard as speech and committed, held or paused, with the room kept under its softest syllable; a voice with a narrow range heard through a minute without a pause; an even sound that stops committed, as a cough is; a steady hum learned within seconds, the turn it made meanwhile cleared as noise, and a voice above it committed, while in a quiet room the same level is speech; a microphone that hisses after a quiet one committing nothing; digital silence not making every sound speech; the turn in progress where the audio stops committed, cleared — too short, or as even as a tone — or nothing, and a reset forgetting it |
| `packages/filmopen_dictation/test/dictation_controller_test.dart` | the session controller on the fakes (§9.6): the chain — `started` with a one-minute estimate and the key's last four, `running` after each full minute, `succeeded` with six-place minutes, every later record naming the first; no key, a stored value too short or not JSON, and an unusable store, starting nothing; a refused microphone costing nothing; a refused session ending `failed` with its code and no cost; a dropped connection; a microphone failing while recording; *Stop* and *Resume* in one chain; *Stop* answering at once, and a session ending while the microphone stops keeping its end; a second *Resume* starting no second microphone; a session closed before it was accepted ending `canceled` with no cost, and one lost unaccepted with no reason ending `failed` `connection`; a close while the key, the microphone or the first record is on its way stopping it there, logging nothing, and nothing starting after it; a microphone chosen while listening taking over at once, and one chosen while stopped or while the session opens recording from the next start; *Resume* doing nothing while a session ends; a close while the log answers turning the microphone off at once; the app's exit ending a dictation still listening and waiting for its last record, even from a platform that never answers the close; a platform file that takes its key other than as a bearer token refused; five minutes without speech, then a new chain with the text kept; the limit's warning and end; an unwritable log stopping everything before anything is sent; the device deciding the turns: speech then quiet committed once and the quiet after it cleared, and nothing committed before the session opens; nothing committed or cleared once a session has ended, even with speech in progress; *Stop* committing the turn in progress, and `settle` waiting for its final text, at most three seconds, and not waiting again for what got no answer; a refused commit and a turn that could not be transcribed waiting for nothing, and no turn waiting past its session's end; speech the device hears keeping a session open past five minutes, with a turn committed every 30 s of talk, and quiet ending it; a session turned down logging the platform's codes and field, never its message; the key in no record and no log line, and the log saying, in numbers, how the device heard each session, each line counting only its own; the minutes' arithmetic; the setup read from the bundled catalogue, refusing an endpoint off OpenAI's hosts, one that is not `wss`, one with anything before the host, and an entry with no price for a minute |
| `packages/filmopen_dictation/test/dictation_popup_test.dart` | the microphone and the popup on a box of their own (§9.6): the microphone and its menu on a box that can be typed into and none on one that cannot or on the web; a blank box listening at once, its text's label at the top, and *Insert* going through the box's `onChanged`, the chain ending `succeeded` and naming the box; a box with text asking first, *AI rewrite* greyed with its reason, the apply button reading *Apply* and no count until a choice, *Insert at cursor* at the caret, and *Replace*; a box whose rules keep nothing left as it was, saying so, and a box at its limit saying that instead; *Cancel* before and after listening; *Stop* and *Resume*, and *Resume* waiting, with its reason, while a session's last record is on its way; Escape as *Cancel*, over a held popup too; no key, with no count, opening Provider keys, and from a box in a dialog closing the dialog too, while a dialog guarding its changes stays and the page with it; a refused microphone; a refused key offering no *Resume*; the menu listing the microphones when it opens and keeping the choice and the switch, and a microphone chosen in the popup recording from then on in the same session and chain; the menu checking the system's microphone when the one chosen is gone, and opening once for two quick presses; *Hold to record* from press to release, the turn in progress committed and its final text applied, with no menu over the held popup and the doors and the status line saying why they wait while it applies, and the status line saying the session is ending while the popup closes; *Insert* while listening committing the turn in progress and putting in its final text rather than the half shown; a hold the system cancels, or the switch turned off, stopping the audio and applying nothing until a button says so; a box that goes, tapped or held, taking nothing while the popup says so and keeps the text; a release with nothing said closing, and one on a failure staying; a release before the session opened, with nothing sent or said, closing the popup and ending the chain `canceled`; edited text staying the person's while later turns follow it; the popup at 700 and 375 px, light and dark, English and Spanish; `applyDictation`'s caret, length-limit and formatter rules, *Replace* on a box already at its limit included |
| `packages/filmopen_platform/test/audio_source_test.dart` | the microphone seam (§9.6): a frame is 100 ms of 24 kHz mono PCM16, 4,800 bytes; no microphone until an entrypoint installs one; the fake source's two devices, the one a recording asked for kept and a device no longer offered falling back to the default; a fake recording giving a frame per interval until it is stopped, stopping twice harmless, silent unless told how loud each frame is, and then a square wave of that loudness; a microphone that cannot start saying why, once; a recording failing as a microphone taken away does; the framer cutting chunks of any size into 100 ms frames, keeping its own copies and passing errors on |
| `packages/filmopen_platform/test/app_paths_test.dart` | the folder doors on a desktop: the person names the folder, a file can be shown, and the two answers are one decision |
| `packages/filmopen_platform/test/incoming_links_test.dart` | the link door: nothing installed answers nothing; the memory door answers its initial link to every caller and delivers what it is handed |

Where a test lives: with the code it proves — `dart test` in the pure packages, `flutter test` inside a Flutter package — unless it pumps the whole application, in which case it stays in the root `test/`; `scripts/test_all.sh` (or `test_all.ps1`) runs every suite. Widget tests load projects in `setUpAll` because file IO does not complete inside a widget test's fake-async zone. Tree rows draw label and detail as one rich text; match them with `find.text('label detail', findRichText: true)`.

`flutter analyze` and `flutter test` must both be clean before a commit. Tests that need `dart:io` import the `_io` file directly (`log_file_io.dart`), because the analyzer resolves a conditional export to its stub.
