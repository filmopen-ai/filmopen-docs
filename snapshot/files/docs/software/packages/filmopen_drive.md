# filmopen_drive

*Generated by `scripts/package_digest.py`; regenerate, never edit.*

Google Drive as a place a project's media can live: the store, its sign-in, and the panel *New project* shows for it.

**Flutter.** 15 files, 2,077 lines under `lib/`; 89 tests in 6 files. Barrel: `lib/filmopen_drive.dart`.

## Depends on

- **filmopen_format** — uses `DataLocator`, `LocatorType`, `ProjectSource`, `StaleFileException`
- **filmopen_media** — uses `MediaCache`, `MediaStore`, `Playable`, `PlayableBytes`, `PlayableFile`, `RootPlan`, `StoreProblem`
- **filmopen_media_ui** — uses `Function`, `MediaRootChoice`, `MediaRootController`, `MediaSourceUi`
- **filmopen_store** — uses `AppLog`
- **filmopen_l10n** — uses nothing by name the digest could see

Third-party: googleapis_auth, flutter_secure_storage, shared_preferences, url_launcher, path_provider, http, crypto.

## Used by

`filmopen_app`

## Public API

What the barrel exports, file by file. A name is `class`, `enum`, `mixin`, `extension`, `typedef`, a top-level function or variable; under a class, its public constructors, fields, getters, setters and methods.

### `lib/src/drive_auth.dart`

- **class `DriveAuth`** — The application's way in and out of a Google account.
  - `DriveAuth({ DriveTokenStore? tokens, DriveClientConfig? config, http.Client Function()? httpClient, DriveSignIn? signIn, DriveRefresh? refresh, thi…`
  - `final DriveRedirects? redirects` — The links the operating system hands the app, where the build has a door: on Android the consent page's redirect arrives there and nowhere else (milestone 5.01, step 3).
  - `DriveClientConfig get config`
  - `bool get isConfigured` — Whether this build could sign in at all (hand-off A).
  - `Future<bool> get isSignedIn` — Whether somebody has signed in on this machine.
  - `Future<void> signIn()` — Opens the consent page and keeps what comes back.
  - `Future<void> signOut()` — Forgets the tokens.
  - `Future<http.Client> client()` — An `http.Client` that carries a live access token, refreshing it where it has run out.

### `lib/src/drive_cache_stub.dart`

- **function** `Future<MediaCache?> openDriveCache()` — No folder of our own here: `open` answers the bytes.

### `lib/src/drive_client.dart` — only DriveClient, DriveError, DriveException, DriveFile

- **enum `DriveError`** — Why a call to Drive did not do what was asked.
  - values: `unconfigured`, `refused`, `notFound`, `alreadyThere`, `lengthUnknown`, `busy`, `unavailable`
- **class `DriveException`** — What a Drive call refused to do, as a code.
  - `const DriveException(this.code)`
  - `final DriveError code`
- **class `DriveFile`** — One file or folder of a Drive, as much of it as FilmOpen uses.
  - `const DriveFile({required this.id, required this.name, this.isFolder = false, this.size})`
  - `final String id`
  - `final String name`
  - `final bool isFolder`
  - `final int? size` — The bytes, where Drive reported them: a folder has none, and so does a file whose metadata was not asked for.
  - `static DriveFile? fromJson(Object? json)`
- **class `DriveClient`** — The calls, over whatever client the caller hands in.
  - `DriveClient(this.client, {Uri? api, Uri? uploads})`
  - `final http.Client client` — A client that already carries the token, or a stand-in.
  - `void close()` — Gives the connections back.
  - `final Uri api` — Where the calls go.
  - `final Uri uploads` — Where the bytes of an upload go, which is a host of its own.
  - `Future<List<DriveFile>> childrenOf(String parentId, {bool foldersOnly = false})` — The folders and files directly inside [parentId] (`root` for *My Drive*).
  - `Future<DriveFile?> childNamed(String parentId, String name)` — The one file of this name directly inside [parentId], or null.
  - `Future<DriveFile?> fileById(String id)` — One file or folder by its id, or null where there is no such thing any more — which is what a remembered id has to be asked, since listing the children of a folder that is gone answers an empty list rather than a refu…
  - `Future<DriveFile> createFolder({required String parentId, required String name})`
  - `Future<DriveFile> upload({ required String parentId, required String name, required Stream<List<int>> bytes, required int? length, String? mimeType…` — Uploads [bytes] as a new file of [name] under [parentId].
  - `Future<http.ByteStream> download(String id)` — The bytes of one file, as they arrive.
  - `Future<void> delete(String id)`

### `lib/src/drive_config.dart`

- **variable** `const String kDriveScope` — The one scope FilmOpen asks for.
- **variable** `const String kDriveAppFolder` — The folder FilmOpen makes for itself under *My Drive*, which every project folder it creates goes under.
- **class `DriveClientConfig`** — The OAuth client of a build.
  - `const DriveClientConfig({required this.id, required this.secret, this.androidId = ''})`
  - `final String id` — The client id of a **Desktop app** client of the owner's Google Cloud project, or empty in a build that has none.
  - `final String secret` — The desktop client's secret, which Google does not treat as one.
  - `final String androidId` — The client id of an **Android** client of the same project, or empty in a build that has none (milestone 5.01, step 3).
  - `String clientIdFor(TargetPlatform platform)` — The client id a build on [platform] signs in with.
  - `String? clientSecretFor(TargetPlatform platform)` — What a build on [platform] may send as the client's secret: nothing at all on Android, Google issuing an Android client none.
  - `bool isConfiguredFor(TargetPlatform platform)` — Whether a build on [platform] has the client it needs.
  - `bool get isConfigured` — Whether this build has a client at all.
  - `static const DriveClientConfig current` — **From the build, never from this file.** The id and the secret are the owner's, they must not reach git, and a file in the repository is no place for them however public Google considers a desktop client's secret to…

### `lib/src/drive_consent.dart`

- **variable** `final Uri kGoogleAuthorizationEndpoint` — Google's own consent endpoint for an installed application.
- **function** `Uri driveConsentUri({ required String clientId, required String redirectUri, required String scope, required String codeVerifier, required String s…` — The address to open in the person's own browser.
- **function** `String androidRedirectUri(String clientId)` — Google's redirect for an **Android** OAuth client: the client id reversed, as a scheme of its own, with a path Google's own examples use (milestone 5.01, step 3).
- **function** `String androidRedirectScheme(String clientId)` — The scheme alone, which is what the manifest declares.
- **function** `Future<String> codeFromDriveRedirect(Stream<Uri> links, { required String scheme, required String state, Duration within = const Duration(minutes:…` — The code in the first redirect **of ours** to arrive on [links], within [within].
- **function** `String codeChallengeFor(String verifier)` — The S256 challenge of a verifier: base64url of its SHA-256, without padding (RFC 7636 §4.2).
- **function** `String newCodeVerifier()` — 128 unreserved characters from a secure source (RFC 7636 §4.1).
- **function** `String newState()` — What ties the answer to this request, so that a reply for somebody else's is refused.

### `lib/src/drive_media_store.dart`

- **class `DriveFolderMemory`** — Where the id of a project's Drive folder is remembered, per machine.
  - `Future<String?> idFor(String path)`
  - `Future<void> remember(String path, String id)`
  - `Future<void> forget(String path)` — Forgets one, for a folder that is no longer in the Drive.
- **class `MemoryDriveFolders`** — A memory that forgets when the application closes: the default in a check, and a perfectly correct one anywhere — the path resolves it again.
- **function** `String driveLocatorPath({required String parentPath, required String name})` — Where a new project's folder goes, as one rule in one place: under [parentPath], or under FilmOpen's own folder when the panel offered no parent.
- **class `DriveMediaStore`** — The media root of a project whose locator is `google-drive` (§4.4).
  - `DriveMediaStore({ required this.locatorPath, required this.auth, this.cache, DriveFolderMemory? folders, DriveClient Function(http.Client client)?…`
  - `final String locatorPath` — The manifest's own path: `FilmOpen/<tag>-data`, `/`-separated, under *My Drive*.
  - `final DriveAuth auth`
  - `final DriveFolderMemory folders`
  - `Future<List<DriveFolderChoice>> parentsOffered()` — The folders a new project may be made under: FilmOpen's own folder, and whatever folders FilmOpen has already made inside it.
  - `static ({List<String> folders, String name}) _split(String path)`
- **class `DriveFolderChoice`** — A folder a new project on Drive may be made under, as the panel offers it: the id this machine may use, the path the manifest would record, and the name a person reads.
  - `const DriveFolderChoice({required this.id, required this.path, required this.name})`
  - `final String id`
  - `final String path`
  - `final String name`
- **class `SharedPreferencesDriveFolders`** — The folder ids this machine remembers, in the application's own preferences (§4.4's per-machine substitute for a locator).
  - `const SharedPreferencesDriveFolders()`

### `lib/src/drive_source_ui.dart`

- **class `DriveSourceUi`** — Google Drive as a place a project's media can live.
  - `DriveSourceUi({required this.auth, required this.parentsOffered})`
  - `final DriveAuth auth`
- **function** `String driveReason(AppLocalizations l10n, DriveError code)` — What a Drive code says to a person.

### `lib/src/drive_tokens.dart`

- **class `DriveTokens`** — What a sign-in leaves behind.
  - `const DriveTokens({required this.accessToken, required this.expiresAt, required this.refreshToken})`
  - `final String accessToken`
  - `final DateTime expiresAt` — In UTC, as Google gives it.
  - `final String? refreshToken` — Null where Google gave none back — a second consent in the same hour, for one — in which case the stored one is kept.
  - `bool get isFresh` — A minute's grace, so a token that is about to expire is refreshed before a large upload rather than in the middle of one.
  - `Map<String, Object?> toJson()`
  - `static DriveTokens? fromJson(Object? json)`
- **class `DriveTokenStore`** — Where the tokens are kept.
  - `Future<DriveTokens?> read()`
  - `Future<void> write(DriveTokens tokens)`
  - `Future<void> clear()`
- **class `SecureDriveTokenStore`** — The platform's own credential store.
  - `const SecureDriveTokenStore([this.storage = const FlutterSecureStorage()])`
  - `final FlutterSecureStorage storage`
- **class `MemoryDriveTokenStore`** — A token store in memory, for a check.
- **typedef** `typedef DriveSignIn` — Opening the consent page and coming back with tokens.
- **typedef** `typedef DriveRedirects` — The links the operating system hands the app, as the consent flow needs them (milestone 5.01, step 3).
- **typedef** `typedef DriveRefresh` — Asking Google for a fresh access token with a refresh token.
- **function** `Future<DriveTokens> exchangeCodeWithGoogle(DriveClientConfig config, String code, { required String verifier, required String redirectUri, required…` — The code exchange on Android, which FilmOpen writes itself.
- **variable** `const String kGoogleTokenEndpoint` — Google's token endpoint, which both the exchange and the refresh post to.
- **function** `Future<DriveTokens> refreshThroughGoogle(DriveClientConfig config, String refreshToken, http.Client client, { TargetPlatform? platform, })` — The refresh call, which is plain HTTP on every platform.

## Internal

Files under `lib/` the barrel does not reach; nothing outside the package may import them.

- `lib/src/drive_cache_io.dart`: `openDriveCache`
- `lib/src/drive_folders.dart`: `folderNamed`
- `lib/src/drive_sign_in.dart`
- `lib/src/drive_sign_in_io.dart`: `kDriveConsentTimeout`, `signInThroughBrowser`, `_openInTheBrowser`, `_throughTheLoopback`, `_throughTheLinkDoor`, `_consentThen`, `_exchange`, `_codeFromRedirect`
- `lib/src/drive_sign_in_stub.dart`: `kDriveConsentTimeout`, `signInThroughBrowser`
