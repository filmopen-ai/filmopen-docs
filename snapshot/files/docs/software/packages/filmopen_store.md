# filmopen_store

*Generated by `scripts/package_digest.py`; regenerate, never edit.*

Where a project's files come from and how they are written, as pure Dart: the folder source (`directory_source*.dart`, `dart:io` behind a stub), the loader, **the writer** (`project_writer.dart`) with its refusals, the creator and the template, `Guards` (development mode's policy, §4.5 of the refactor plan), the log (`app_log.dart`, `log_file*.dart`) and the library install.

**pure Dart.** 21 files, 2,651 lines under `lib/`; 245 tests in 13 files. Barrel: `lib/filmopen_store.dart`.

## Depends on

- **filmopen_format** — uses `Author`, `BatchStem`, `ChildEntry`, `Commentary`, `CommentaryStem`, `DataLocator`, `DuplicateKeyException`, `Entity`, `EntityGroup`, `EntityOrigin`, `EntityType`, `EntityVersions`, `EpochDeclaration`, `FileMedia`, `FileNameParser`, `FilmProject`, `LocatorType`, `MediaLocation`, `OfficialPointer`, `OfficialStem`, `ProjectManifest`, `ProjectSource`, `ProjectWarning`, `RenderBatch`, `ShortName`, `SkippedUsageLine`, `StaleFileException`, `Take`, `TakeName`, `UnknownName` and 27 more
- **filmopen_test_support (dev)** — uses nothing by name the digest could see

## Used by

`filmopen_account`, `filmopen_app`, `filmopen_automation`, `filmopen_browser`, `filmopen_compare`, `filmopen_device`, `filmopen_dictation`, `filmopen_drive`, `filmopen_forms`, `filmopen_insertion`, `filmopen_keys`, `filmopen_media`, `filmopen_media_ui`, `filmopen_platform`, `filmopen_plugin_host`, `filmopen_recorder`, `filmopen_state`, `filmopen_test_support`, `filmopen_tree`

## Public API

What the barrel exports, file by file. A name is `class`, `enum`, `mixin`, `extension`, `typedef`, a top-level function or variable; under a class, its public constructors, fields, getters, setters and methods.

### `lib/src/app_log.dart`

- **enum `LogLevel`** — Verbosity, least to most.
  - values: `error`, `warning`, `info`, `debug`, `trace`
  - `static LogLevel? fromName(String name)`
- **class `LogEntry`** — One line of the log.
  - `LogEntry(this.level, this.event, {this.data = const {}, this.error, this.stackTrace, DateTime? time})`
  - `final DateTime time`
  - `final LogLevel level`
  - `final String event` — A stable dotted name, e.g. `project.opened`, `render.failed`.
  - `final Map<String, Object?> data` — Structured detail.
  - `final Object? error`
  - `final StackTrace? stackTrace`
  - `Map<String, Object?> toJson()`
  - `String toLine()` — The JSON line, without its newline.
- **class `LogSink`** — Where log lines go.
  - `void write(LogEntry entry)`
  - `Future<void> close()` — Flushes and releases resources.
- **class `MemoryLogSink`** — Keeps entries in memory, for tests.
  - `final List<LogEntry> entries`
- **class `LineLogSink`** — Hands each line to a callback: the console, a test.
  - `const LineLogSink(this.onLine)`
- **class `AppLog`** — The application log.
  - `AppLog({this.threshold = LogLevel.info})`
  - `static final AppLog instance`
  - `LogLevel threshold` — Entries above this level are dropped before reaching any sink.
  - `bool thresholdPinned` — True when the environment (`FILMOPEN_LOG_LEVEL`) fixed the threshold, so the *verbose logs* setting must not override it.
  - `void pinThreshold(LogLevel level)` — Fixes [threshold] for the run; see [thresholdPinned].
  - `void applyVerbose(bool verbose)` — Applies the *verbose logs* setting unless the environment pinned a level: verbose is [LogLevel.debug], otherwise [LogLevel.info].
  - `String? filePath` — Where the file sink writes, when one is attached; shown in Settings.
  - `List<LogSink> get sinks`
  - `void addSink(LogSink sink)`
  - `void removeSink(LogSink sink)`
  - `bool isEnabled(LogLevel level)`
  - `void log(LogLevel level, String event, {Map<String, Object?> data = const {}, Object? error, StackTrace? stackTrace})`
  - `void error(String event, {Map<String, Object?> data = const {}, Object? error, StackTrace? stackTrace})`
  - `void warning(String event, {Map<String, Object?> data = const {}, Object? error})`
  - `void info(String event, {Map<String, Object?> data = const {}})`
  - `void debug(String event, {Map<String, Object?> data = const {}})`
  - `void trace(String event, {Map<String, Object?> data = const {}})`
  - `Future<T> timed<T>(String event, Future<T> Function() body, {LogLevel level = LogLevel.debug, Map<String, Object?> data = const {}})` — Runs [body], logging how long it took at [level] as `<event>` with a `ms` field, or `<event>.failed` at error level when it throws.
  - `Future<void> close()`

### `lib/src/guards.dart`

- **enum `GuardRule`** — A refusal the development switch governs.
  - values: `notYourFile`, `notDirector`, `libraryFile`
- **class `Guards`** — The policy the writer consults before refusing a [GuardRule].
  - `const Guards({this.warnOnly = false})`
  - `static const Guards enforce` — Every guard refuses: a release build, and any caller that did not ask.
  - `static const Guards warn` — Development mode: a governed guard warns instead of refusing.
  - `final bool warnOnly`
  - `void check(GuardRule rule, Exception Function() refusal)` — Throws what [refusal] makes when enforcing; in development mode lets the action through.
  - `void report(Iterable<GuardRule> rules, {Map<String, Object?> data = const {}})` — Logs `guard.overridden` for each rule a write went ahead in spite of, with [data] (codes and handles, never text a person wrote).

### `lib/src/library_install_stub.dart`

- **function** `Future<int> installLibraryIfEmpty(String directory, ProjectSource seed)` — No files on this platform: the bundled copy is read in place.

### `lib/src/log_file_stub.dart`

- **function** `LogSink? openFileLogSink(String path)` — No files on this platform: the console sink is the only one.

### `lib/src/project_creator_stub.dart`

- **variable** `const bool canCreateProjects` — Whether this platform can create project folders on disk.
- **class `ProjectFolderNotEmptyException`**
  - `const ProjectFolderNotEmptyException(this.path)`
  - `final String path`
- **class `ProjectFolderIsLinkException`**
  - `const ProjectFolderIsLinkException(this.path)`
  - `final String path`
- **class `NotAProjectFolderException`**
  - `const NotAProjectFolderException(this.path)`
  - `final String path`
- **function** `Future<String> createProject({ required String parentPath, required String tag, required Author author, String? name, required String epochLabel, r…`
- **function** `Future<String> copyProjectTo({ required String parentPath, required String folder, required Map<String, String> files, })`
- **function** `Future<void> deleteProjectFolder(String path)`
- **function** `bool isDeletableProjectFolder(String path)`
- **function** `bool sameFolderPath(String a, String b)`
- **function** `String? mediaFolderOf(String projectPath)`
- **function** `bool isDeletableMediaFolder(String mediaPath, String projectPath, {Iterable<String> protectedPaths = const []})`
- **function** `Future<void> deleteMediaFolder(String mediaPath, String projectPath, {Iterable<String> protectedPaths = const []})`

### `lib/src/project_loader.dart`

- **class `ProjectLoader`** — Reads every file a [ProjectSource] lists, parses its name by the grammar of §5.4, and builds the [FilmProject] index.
  - `const ProjectLoader()`
  - `Future<FilmProject> load(ProjectSource source, {ProjectSource? library})`

### `lib/src/project_template.dart`

- **variable** `const JsonEncoder projectJsonEncoder` — Two-space indentation and a final newline, as §18.1 asks of writers.
- **function** `String encodeProjectJson(Object? json)`
- **variable** `const String initialEpochTag` — The token of the one epoch a new project starts with (§8.2).
- **variable** `const String firstSceneTag` — The short name of the scene a new project starts with (milestone 5.11, the plan's §3.13): the number *Add scene…* gives a project's first scene.
- **function** `Future<String> writeNewProject(ProjectSource target, { required String tag, required Author author, String? name, required String epochLabel, requi…` — Writes `filmopen-project.json`, `sc_1_<author>_v1.json` and `pj_<tag>_<author>_v1.json` into [target], which must be empty.
- **function** `String rfc3339Now([DateTime? now])` — RFC 3339 UTC to the second (§7 `created` / `updated`), without Dart's microseconds.

### `lib/src/project_writer.dart`

- **class `ProjectReadOnlyException`** — The project's files cannot be written (bundled assets).
  - `const ProjectReadOnlyException(this.label)`
  - `final String label`
- **class `NotYourFileException`** — The file belongs to another author; edit a fork instead (§17 rule 2).
  - `const NotYourFileException(this.stem, this.owner, this.viewer)`
  - `final String stem`
  - `final Author owner` — Who the file belongs to.
  - `final Author? viewer`
- **class `NoViewerException`** — Writing without a viewer handle: there is no author to write as.
  - `const NoViewerException()`
- **class `NotDirectorException`** — Only a director of the project writes official pointers (§8.3, §13.2).
  - `const NotDirectorException(this.viewer)`
  - `final Author? viewer`
- **class `IdentityChangedException`** — A save tried to change what names the file (§7): `filmopen`, `type`, `tag`, `epoch`, `author` or `v`.
  - `const IdentityChangedException(this.key)`
  - `final String key`
- **class `EntityExistsException`** — A new entity was asked for under a tag its type already has, in the project or in the shared library (§5.2: the tag is the entity; a second file under it is a version, and a version is made by forking).
  - `const EntityExistsException(this.groupKey)`
  - `final String groupKey` — `<type>_<tag>`, e.g. `ch_main-hero`.
- **class `PluginOutputException`** — What a plug-in's entity hook returned cannot be written (§14.4): [code] is `noSource` — an entity whose type and tag match none the hook was given.
  - `const PluginOutputException(this.code)`
  - `final String code`
- **class `PluginRun`** — A plug-in's run, as its files record it (Project Specification §12.4, §14.4): the plug-in's [tag] — a run's files are written under the running person's derived handle `<viewer>.<tag>` (§5.6) — its [stem], the [hook]…
  - `const PluginRun({required this.tag, required this.stem, required this.hook, this.settings, this.item = const {}})`
  - `final String tag`
  - `final String stem`
  - `final String hook`
  - `final Map<String, Object?>? settings` — The project-scoped settings the run had, for an entity hook: its batch records them, empty where it had none (§12.4).
  - `final Map<String, Object?> item` — What **each take's item** records of a render's request (§12.2; milestone 5-2b §3.5): `model`, `platform`, `prompt`, `params` with the seed and `inputs`, **as the application prepared them**, and from the plug-in's an…
  - `Author runner(Author viewer)` — The handle the run's files are written under (§5.6, §14.4).
  - `Map<String, dynamic> get batchFields` — The batch record's own fields for the run (§12.4).
- **variable** `const List<String> identityKeys` — The header fields that must equal the filename's (§7).
- **enum `ContentProblem`** — What a file must say to be worth writing (§7, §8.2, §18.3).
  - values: `nameRequired`, `versionLabelTooLong`, `epochsRequired`, `childrenNotAList`, `folderIntoItself`
- **class `InvalidContentException`**
  - `const InvalidContentException(this.problem)`
  - `final ContentProblem problem`
- **function** `void validateContent(EntityType type, Map<String, dynamic> json)` — Refuses content the format requires otherwise: a missing `name`, an over-long `versionLabel`, a project without epochs.
- **class `ProjectWriter`**
  - `const ProjectWriter(this.project, {this.guards = Guards.enforce})`
  - `final FilmProject project`
  - `final Guards guards` — Whether the rules about who may write warn instead of refusing (development mode, §9.5).
  - `Future<void> saveEntity(Entity entity, Map<String, dynamic> json, {required Author? viewer})` — Replaces the content of [entity]'s file with [json], setting `updated`.
  - `void checkSave(Entity entity, Map<String, dynamic> json, {required Author? viewer})` — Whether [saveEntity] would refuse [json] as the new content of [entity] for a reason that can be known before anything is written — who may write it, the identity the filename owns, a project file's provenance, the co…
  - `Future<VersionStem> fork(Entity entity, {required Author? viewer})` — Fork (§13.1): on the viewer's own version, the next number; on someone else's, the viewer's next number (v1 when they have none).
  - `Future<VersionStem> createEntity({ required EntityType type, required String tag, String? epoch, required String name, String? kind, required Autho…` — A new entity as the viewer's first version (§9, §10, §13.1): a library entity — a character, a location — at [epoch], `<type>_<tag>_<epoch>_<author>_v1.json`, or a **folder** or a **scene** (§10.1, §10.2; [childTypes]…
  - `Future<List<String>> writePluginRun({ required PluginRun run, required List<Entity> sources, required List<Map<String, dynamic>> outputs, required…` — Writes what a plug-in's entity hook returned (Project Specification §14.4, §12.4, §5.6): each entity as a new version under the running person's derived handle `<viewer>.<tag>` — `v1` for the first run, the next numbe…
  - `Future<RenderBatch> writeImportBatch({ required BatchStem stem, required List<Map<String, dynamic>> items, required Author? viewer, Map<String, dyn…` — Writes the record of one import action (§12.3): `rd_<id>_<author>.json` beside the project's files, `kind: "import"`, one item per file.
  - `Future<void> setOfficial(Entity entity, {required Author? viewer})` — Writes the official pointer for [entity]'s versions key (§13.2) naming it, with `setBy` and `setAt`.
  - `Future<void> clearOfficial(Entity entity, {required Author? viewer})` — Removes the official pointer for [entity]'s versions key, so the project has no chosen version there.
  - `Future<void> setPick(EntityVersions versions, {required Author? viewer, Entity? entity, bool trackOfficial = false})` — Records the viewer's pick at [versions]' epoch — `pick` and `pickedAt` in the viewer's own commentary file for the entity (§13.4): [entity] pins that version; [trackOfficial] follows the official pointer, whichever ve…

### `lib/src/usage_log.dart`

- **class `UsageLogUnavailable`** — A usage log that could not do what was asked, with a code: `unavailable` (no log is installed, or this platform keeps none), `lock_timeout`, `io` or `device_unreadable`.
  - `const UsageLogUnavailable(this.code)`
  - `final String code`
- **class `UsageReading`** — What a reading of the log found (§6.7, *Reading*).
  - `const UsageReading({this.records = const [], this.skippedChains = const {}, this.complete = true})`
  - `final List<UsageRecord> records` — Every record read, in file and line order.
  - `final Set<String> skippedChains` — The chains holding a record of a version this reader does not know, left out whole.
  - `final bool complete` — False when a month file could not be read, so that a total can say it may be short.
  - `List<UsageChain> get chains` — The calls read.
  - `static void addLines(String file, Iterable<String> lines, { required List<UsageRecord> records, required Set<String> skippedChains, required Set<St…` — Reads the [lines] of the month file [file] into [records] and [skippedChains].
- **class `UsageLog`** — Where usage records go (§6.7).
  - `const UsageLog()`
  - `static UsageLog current` — The log in use.
  - `static void install(UsageLog log)` — Makes [log] the one in use.
  - `bool get isAvailable` — Whether records can be written at all.
  - `Future<String> deviceId()` — The id in the usage folder's `device.json`, which is made on first use.
  - `Future<void> append(UsageRecord record)` — Appends [record], or throws [UsageLogUnavailable].
  - `Future<UsageReading> read()` — Every record in the log.
  - `Stream<void> get changes` — An event after each record this app appends.
  - `Future<void> appendUntilWritten(UsageRecord record, {Duration firstWait = const Duration(milliseconds: 250)})`
- **class `UnavailableUsageLog`** — No usage log: nothing is written, so no paid call starts.
  - `const UnavailableUsageLog()`
- **class `MemoryUsageLog`** — A usage log in memory, for the tests and the agent build, which must not write the user's usage folder (milestone 4.9 plan, D10).
  - `MemoryUsageLog({this.device = '00000000-0000-4000-8000-000000000000'})`
  - `final String device` — The id [deviceId] answers.
  - `final List<String> lines` — The lines written, oldest first, without their line feeds.
  - `int failing` — How many of the next appends fail, as a write that cannot take the lock fails.
  - `List<UsageRecord> get records` — The records written.

### `lib/src/usage_log_stub.dart`

- **function** `UsageLog openUsageLog(String folder, { Duration lockTimeout = const Duration(seconds: 10), Duration retryEvery = const Duration(milliseconds: 50),…` — No files on this platform: the web keeps no usage (Software Specification §6.7).

## Internal

Files under `lib/` the barrel does not reach; nothing outside the package may import them.

- `lib/src/directory_source_io.dart`: `canOpenDirectories`, `directoryProjectSource`, `directoryExists`, `ProjectPathException`, `DirectoryProjectSource`
- `lib/src/directory_source_stub.dart`: `canOpenDirectories`, `directoryProjectSource`, `directoryExists`
- `lib/src/library_install_io.dart`: `installLibraryIfEmpty`
- `lib/src/log_file_io.dart`: `FileLogSink`, `openFileLogSink`
- `lib/src/project_creator_io.dart`: `canCreateProjects`, `ProjectFolderNotEmptyException`, `ProjectFolderIsLinkException`, `NotAProjectFolderException`, `createProject`, `_takeBack`, `copyProjectTo`, `deleteProjectFolder`, `isDeletableProjectFolder`, `sameFolderPath`, `_segments`, `_comparable`, +3
- `lib/src/usage_log_io.dart`: `FileUsageLog`, `openUsageLog`
