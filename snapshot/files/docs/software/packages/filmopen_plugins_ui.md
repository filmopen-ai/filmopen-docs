# filmopen_plugins_ui

*Generated by `scripts/package_digest.py`; regenerate, never edit.*

Settings → Plugins (milestone 5.1 plan §3.9–§3.11, Software Specification §9.4): the list of plug-ins with *Install from file…* and the preferred providers, the consent screen, and a plug-in's page — its switch, its settings on this computer, its actions and their answers, *Check* and what it implements (milestone 5-2b), the keys it asks to use and what a call may spend, and its README — all drawn by the app's own widgets from what `filmopen_state`'s seam answers — three of its roles: `PluginCatalogueView` for what is installed, `PluginSettingsControl` for what the person changes, and `PluginInvocation` for an action and *Check*. Only the app imports it.

**Flutter.** 6 files, 1,145 lines under `lib/`; 38 tests in 4 files. Barrel: `lib/filmopen_plugins_ui.dart`.

## Depends on

- **filmopen_format** — uses `EntityType`, `numberText`
- **filmopen_forms** — uses `Function`, `PluginChoiceField`, `PluginServerBox`, `PluginSwitchField`, `PluginTextBox`, `pluginChoiceToken`, `pluginServerValue`, `pluginSwitchValue`
- **filmopen_state** — uses `FieldRegistry`, `FieldSetter`, `PluginAction`, `PluginCallControl`, `PluginCallDone`, `PluginCallFailed`, `PluginCatalogueView`, `PluginCheckFailed`, `PluginCheckResult`, `PluginChecked`, `PluginField`, `PluginFieldType`, `PluginInvocation`, `PluginOrigin`, `PluginProvider`, `PluginSaysNothing`, `PluginSettingsControl`, `PluginStatusState`, `PluginSummary`, `Settings`
- **filmopen_ui** — uses `EmptyState`, `SectionCard`, `showMessage`
- **filmopen_l10n** — uses nothing by name the digest could see

Third-party: file_selector.

## Used by

`filmopen_app`

## Public API

What the barrel exports, file by file. A name is `class`, `enum`, `mixin`, `extension`, `typedef`, a top-level function or variable; under a class, its public constructors, fields, getters, setters and methods.

### `lib/src/plugins_tab.dart`

- **typedef** `typedef ArchiveChooser` — Chooses a plug-in's zip and answers its bytes; null when the person cancels.
- **function** `Future<Uint8List?> chooseArchive(BuildContext context)` — The platform's file dialog, filtered to zips.
- **class `PluginsTab`** — Settings → Plugins (milestone 5.1 plan §3.9–§3.11, Software Specification §9.4): the plug-ins found in the library and the open project, each with its switch, and a plug-in's own page — its machine settings, actions,…
  - `const PluginsTab({super.key, this.webBuild = kIsWeb, this.chooseFile = chooseArchive, this.openDeveloperPage})`
  - `final bool webBuild` — The web build runs no plug-ins (plan §3.6): the tab is one sentence there.
  - `final ArchiveChooser chooseFile` — How *Install from file…* picks a zip; a test passes its own.

## Internal

Files under `lib/` the barrel does not reach; nothing outside the package may import them.

- `lib/src/consent_dialog.dart`: `askConsent`
- `lib/src/plugin_card.dart`: `setPluginEnabled`, `pluginOriginText`, `PluginEnabledSwitch`, `pluginNotices`, `PluginCard`
- `lib/src/plugin_page.dart`: `_kindWords`, `PluginPage`, `_PluginPageState`
- `lib/src/preferred_providers.dart`: `PreferredProviders`, `_PreferredPullDown`
