# filmopen_media_ui

*Generated by `scripts/package_digest.py`; regenerate, never edit.*

What media looks like, and the one door a media source's own user interface comes in by.

**Flutter.** 17 files, 1,602 lines under `lib/`; 29 tests in 3 files. Barrel: `lib/filmopen_media_ui.dart`.

## Depends on

- **filmopen_format** — uses `DataLocator`, `FileMedia`, `FilmProject`, `LocatorType`, `MediaKind`, `isPortableMediaFolder`, `mediaExtensions`
- **filmopen_media** — uses `MediaStore`, `Playable`, `PlayableBytes`, `PlayableFile`, `PlayableUrl`, `ProjectMedia`
- **filmopen_insertion** — uses `Function`, `MediaInput`
- **filmopen_store** — uses `AppLog`
- **filmopen_platform** — uses `revealInFileManager`
- **filmopen_ui** — uses `Breakpoints`, `EmptyState`, `imageProviderFor`
- **filmopen_l10n** — uses nothing by name the digest could see

Third-party: file_selector, video_player_win.

## Used by

`filmopen_app`, `filmopen_drive`, `filmopen_forms`, `filmopen_tree`

## Public API

What the barrel exports, file by file. A name is `class`, `enum`, `mixin`, `extension`, `typedef`, a top-level function or variable; under a class, its public constructors, fields, getters, setters and methods.

### `lib/src/file_picker_stub.dart`

- **variable** `const bool canChooseFiles` — The web has no files of its own to name: media reaches a session copy through the URL door (D21), which is also how a check uploads.
- **function** `Future<List<String>> chooseMediaFiles(BuildContext context)`

### `lib/src/folder_picker_stub.dart`

- **function** `Future<String?> chooseFolderPath(BuildContext context, {String? startAt})`

### `lib/src/media_root_picker.dart`

- **class `MediaRootPicker`**
  - `const MediaRootPicker({ super.key, required this.controller, required this.parentPath, required this.enabled, this.chooseFolder, this.canBrowse, })`
  - `final MediaRootController controller` — What the picker settles on, and what *New project* reads.
  - `final String parentPath` — The folder the project itself is going into, so the proposal can sit beside it.
  - `final bool enabled`
  - `final bool? canBrowse` — Whether a *Browse* button stands beside the folder row: the platform's own answer ([canChooseFolder]) unless the caller knows better — a dialog pumping a phone's answers in a test (milestone 5.01, D5).

### `lib/src/media_source_ui.dart`

- **class `UploadDoor`** — A door the upload sheet offers for one source: a button that answers what to import, or null when the person changed their mind.
  - `const UploadDoor({ required this.identifier, required this.label, required this.icon, required this.open, })`
  - `final String identifier` — The semantics identifier an agent drives it by (`media.<type>.<door>`).
  - `final String label`
  - `final IconData icon`
- **class `MediaSourceUi`** — The user interface of one media source (§4.4's locator types).
  - `LocatorType get type`
  - `String label(AppLocalizations l10n)` — What a person calls this place.
  - `IconData get icon`
  - `Widget rootPanel(BuildContext context, MediaRootController controller)` — What *New project* shows for this kind of root: where the folder goes and what it is called, in this source's own terms.
  - `List<UploadDoor> uploadDoors(BuildContext context, FilmProject project)` — The buttons the upload sheet adds for this source.
  - `Widget? player(BuildContext context, ProjectMedia media, Playable playable)` — A player of this source's own, or null to use the shared one — which is the usual answer, since a store fetches into the cache and answers a file (D14).
- **class `MediaSourceUis`** — The source user interfaces an application knows.
  - `MediaSourceUis._()`
  - `static void register(MediaSourceUi ui)`
  - `static void unregister(LocatorType type)`
  - `static MediaSourceUi? of(LocatorType type)`
  - `static List<MediaSourceUi> get registered` — Every source that can make a root for a new project, in a stable order.
- **class `MediaRootChoice`** — What *New project* has settled on for the film's media (§4.4).
  - `const MediaRootChoice()`
  - `const factory MediaRootChoice.none()` — The project folder is its own media root: the specification's default, and no locator is written.
  - `const factory MediaRootChoice.folder(String path)` — A folder on this machine, to be made and named in the manifest.
  - `const factory MediaRootChoice.locator(DataLocator locator)` — A root a source has already made for itself.
- **class `NoMediaRoot`**
  - `const NoMediaRoot()`
- **class `LocalMediaRoot`**
  - `const LocalMediaRoot(this.path)`
  - `final String path`
- **class `MadeMediaRoot`**
  - `const MadeMediaRoot(this.locator)`
  - `final DataLocator locator`
- **class `MediaRootController`** — How a source's root panel says what it has settled on, and how *New project* hears it.
  - `MediaRootController({required String tag})`
  - `String get tag` — The project's tag, for a panel that proposes `<tag>-data`.
  - `set tag(String value)`
  - `MediaRootChoice get choice`
  - `set choice(MediaRootChoice value)`
  - `void settle(MediaRootChoice value, {required bool waiting, String? reason})` — What a panel says every time it settles: the place, and whether that place is an answer.
  - `String? get waitingReason` — Why this panel is waiting, where it knows something the dialog does not: a source with nothing to sign in with cannot be waited for, and *Create* says that rather than asking for a choice that cannot be made.
  - `bool get waiting`
  - `set waiting(bool value)`
  - `bool get busy`
  - `set busy(bool value)`

### `lib/src/media_tile.dart`

- **variable** `const double mediaTileEdge` — The edge of a tile.
- **variable** `const double mediaTileGap` — The gap between tiles, and the edge a strip falls back to where two full-size tiles will not fit beside each other: a card in the narrow pane (§9.1) is about 240 px wide, and one tile to a row wastes it.
- **variable** `const double mediaTileEdgeNarrow`
- **class `MediaTile`**
  - `const MediaTile({ super.key, required this.media, required this.project, required this.identifier, this.onTap, this.edge = mediaTileEdge, this.disa…`
  - `final ProjectMedia media`
  - `final FilmProject project`
  - `final String identifier` — `media:<stem>:<n>`: the n-th tile of the version on screen (D20).
  - `final VoidCallback? onTap`
  - `final double edge`
  - `final String? disabledReason` — Why the tile cannot be opened, for its tooltip (§6).
- **class `MediaStrip`** — The tiles a file's references make, with the **+** tile last where the person may add one (D17, D18).
  - `const MediaStrip({ super.key, required this.media, required this.project, required this.stem, this.onOpen, this.onAdd, this.addDisabledReason, this…`
  - `final List<ProjectMedia> media`
  - `final FilmProject project`
  - `final String stem` — The version on screen, which the tiles' identifiers are made from.
  - `final VoidCallback? onAdd` — Null where the person may not add: the tile is not drawn at all, unless [addDisabledReason] says why it cannot be pressed.
  - `final String? addDisabledReason` — Why **+** is disabled, for its tooltip (§6: a disabled control says why).
  - `final String? openDisabledReason` — Why a tile cannot be opened: no store reaches the media root.

### `lib/src/media_upload_sheet.dart`

- **function** `Future<List<String>> Function(BuildContext context)? debugChooseMediaFiles` — The file dialog the sheet opens.
- **function** `Future<List<MediaInput>> showMediaUploadSheet(BuildContext context, { required bool sessionCopy, List<UploadDoor> doors = const [], Future<List<Str…` — Asks what to import and answers it, or an empty list when the person changes their mind.

### `lib/src/media_viewer.dart`

- **function** `Widget? Function(BuildContext context, ProjectMedia media, Playable playable)? debugMediaPlayer` — The player the viewer opens for a clip or a sound.
- **function** `Future<void> showMediaViewer(BuildContext context, { required ProjectMedia media, required MediaStore store, Widget Function(BuildContext context,…` — Opens [media].

### `lib/src/player/player_controls.dart`

- **class `PlayerNow`** — What a player can say about itself at one moment.
  - `const PlayerNow({required this.position, required this.duration, required this.playing})`
  - `final Duration position` — Where the file has got to.
  - `final Duration duration` — How long the file is, or zero where the player cannot say.
  - `final bool playing`
- **class `PlayerControls`** — Play, pause, where in the file we are and how long it is.
  - `const PlayerControls({ super.key, required this.now, required this.onPlay, required this.onPause, required this.onSeek, })`
  - `final PlayerNow now`
  - `final VoidCallback onPlay` — Play from where the file is, or from the beginning where it has run out: pressing play must always play something.
  - `final VoidCallback onPause`

### `lib/src/player/player_rules.dart`

- **function** `double aspectRatioOrSquare(double reported)` — The ratio to draw a picture at.
- **function** `bool canSeek(Duration duration)` — Whether the seek bar can be dragged at all: a file whose length nobody knows has nowhere to drag to, and a negative length is no length.
- **function** `bool hasRunOut({required Duration position, required Duration duration, required bool completed})` — Whether the file has already run out, so that pressing play must start it again rather than play nothing.
- **function** `String clockText(Duration at)` — `m:ss`, or `h:mm:ss` for anything an hour or longer.

## Internal

Files under `lib/` the barrel does not reach; nothing outside the package may import them.

- `lib/src/file_picker_io.dart`: `canChooseFiles`, `chooseMediaFiles`
- `lib/src/folder_picker_io.dart`: `chooseFolderPath`
- `lib/src/player/media_player.dart`
- `lib/src/player/media_player_io.dart`: `mediaPlayerFor`, `_MediaPlayer`, `_MediaPlayerState`, `_Controls`
- `lib/src/player/media_player_stub.dart`: `mediaPlayerFor`
