# filmopen_ui

*Generated by `scripts/package_digest.py`; regenerate, never edit.*

The theme (`buildTheme`, `AppColors`, `Breakpoints`) and the widgets more than one page shares: `SectionCard`, `InfoChip`, `EmptyState`, `showMessage`, `AttributeView`, `ScreenplayView`, the split view, `ShortNameField`, the logo, the genre picker, the entity icons, the media image.

**Flutter.** 17 files, 1,128 lines under `lib/`; 0 tests in 0 files. Barrel: `lib/filmopen_ui.dart`.

## Depends on

- **filmopen_format** — uses `AssetMedia`, `BytesMedia`, `EntityType`, `FileMedia`, `MediaLocation`, `ShortName`, `ShortNameProblem`, `recommendedGenres`
- **filmopen_l10n** — uses `fieldLabel`

Third-party: multi_split_view.

## Used by

`filmopen_account`, `filmopen_app`, `filmopen_browser`, `filmopen_compare`, `filmopen_dictation`, `filmopen_forms`, `filmopen_keys`, `filmopen_media_ui`, `filmopen_plugin_dev`, `filmopen_plugins_ui`, `filmopen_tree`

## Public API

What the barrel exports, file by file. A name is `class`, `enum`, `mixin`, `extension`, `typedef`, a top-level function or variable; under a class, its public constructors, fields, getters, setters and methods.

### `lib/src/attribute_view.dart`

- **class `AttributeView`** — Renders any JSON object attribute by attribute.
  - `const AttributeView({super.key, required this.data, this.hiddenKeys = const {}})`
  - `final Map<String, dynamic> data`
  - `final Set<String> hiddenKeys`
  - `static Map<String, dynamic> visible(Map<String, dynamic> data, Set<String> hidden)` — [data] without [hidden], dotted paths included.
- **class `AttributeValue`** — One JSON value of any shape.
  - `const AttributeValue(this.value, {super.key})`
  - `final dynamic value`

### `lib/src/empty_state.dart`

- **class `EmptyState`** — A centred icon and message for panes with nothing to show.
  - `const EmptyState({super.key, required this.icon, required this.message, this.detail})`
  - `final IconData icon`
  - `final String message`
  - `final String? detail`

### `lib/src/entity_icons.dart`

- **function** `IconData iconFor(EntityType type)` — Icons per entity type, shared by the tree and the detail pane.

### `lib/src/filmopen_logo.dart`

- **class `FilmOpenLogo`** — The FilmOpen clapperboard, from `assets/branding/` (derived from the master in `assets/branding/source/` by `scripts/make_icons.py`, which also writes the Windows and web icons).
  - `const FilmOpenLogo({super.key, this.size = 24})`
  - `final double size`

### `lib/src/genre_picker.dart`

- **class `GenrePicker`** — A pull-down of check boxes for the project's genres (Appendix A.9): the recommended list plus whatever the file already names, so nothing a hand-edited file says is lost.
  - `const GenrePicker({super.key, required this.selected, required this.onChanged, this.enabled = true})`
  - `final List<String> selected`
  - `final ValueChanged<List<String>> onChanged`
  - `final bool enabled`

### `lib/src/info_chip.dart`

- **class `InfoChip`** — A small, read-only pill: a tag, an author, an epoch, a version.
  - `const InfoChip(this.label, { super.key, this.icon, this.tooltip, this.mono = false, this.emphasis = false, this.outlined = false, this.onTap, })`
  - `final String label`
  - `final IconData? icon`
  - `final String? tooltip`
  - `final bool mono` — Monospace, for stems and tags.
  - `final bool emphasis` — Filled with the primary container colour.
  - `final bool outlined` — Outlined, for a chip sitting on a ground of its own colour — inside a locked field (§9.3), where the fill alone would leave it a loose word.
  - `final VoidCallback? onTap`

### `lib/src/media_image.dart`

- **function** `ImageProvider? imageProviderFor(MediaLocation? location)` — Turns a model-layer [MediaLocation] into something Flutter can paint.

### `lib/src/messages.dart`

- **variable** `const Duration kLongMessage` — How long a message that tells a person **what state their files are in** stays: four seconds is a confirmation's time, not the time it takes to read that a scene is now listed twice, where and why.
- **function** `void showMessage(BuildContext context, String text, { String? actionLabel, VoidCallback? onAction, double width = 420, Duration duration = const Du…` — One place for the app's transient messages, so they all look alike: a floating snackbar, optionally with one action.

### `lib/src/screenplay_view.dart`

- **class `ScreenplayView`** — The blocks of a scene laid out like a screenplay page (§10.3, §10.4).
  - `const ScreenplayView({super.key, required this.blocks, required this.characterName})`
  - `final List<Map<String, dynamic>> blocks`

### `lib/src/section_card.dart`

- **class `SectionCard`** — A titled card grouping related content in the detail pane.
  - `const SectionCard({ super.key, required this.title, required this.child, this.trailing, this.titleRow, this.icon, this.padding = const EdgeInsets.a…`
  - `final String title`
  - `final Widget child`
  - `final Widget? trailing`
  - `final IconData? icon` — A small badge before the title: what the card is about, at a glance.
  - `final Widget? titleRow` — Drawn instead of the title and [trailing], for a card whose title row is laid out on another grid — Compare's, whose `<<` sits over the split rather than at the right edge.
  - `final EdgeInsets padding` — Tightened where two of these sit side by side (§9.3, Compare).
- **class `KeyValueRow`** — A label on the left, a value on the right.
  - `const KeyValueRow(this.label, this.value, {super.key, this.labelWidth = 140, this.tooltip})`
  - `final String label`
  - `final Widget value`
  - `final double labelWidth`
  - `final String? tooltip`

### `lib/src/short_name_field.dart`

- **class `ShortNameField`** — An input box for a short name — a project folder, a tag, an epoch, a handle segment — that enforces the format's segment rule (§5.2) in one place: lowercase `a–z`, `0–9` and dashes, starting with a letter or digit, at…
  - `const ShortNameField({ super.key, this.initialValue = '', required this.onChanged, this.label, this.helperText, this.helperMaxLines = 3, this.autof…`
  - `final String initialValue`
  - `final ValueChanged<String> onChanged` — Called with the filtered text on every change.
  - `final String? label`
  - `final String? helperText` — Shown when there is nothing to warn about.
  - `final int helperMaxLines` — How many lines the helper may take before it is cut: three, unless a dialog has a longer sentence to say at a phone's width.
  - `final bool autofocus`
  - `final ValueChanged<String>? onSubmitted`
  - `final bool allowWorkspace` — Accept an author handle, `owner[.workspace]` (§5.6): one dot, each side a segment.
  - `final bool emptyAllowed` — An empty box is a legitimate answer (Settings: no handle means browsing as nobody), not a problem to explain.
  - `final IconData prefixIcon`
  - `static ShortNameProblem? validate(String value, {bool allowWorkspace = false})` — The problem with [value] under this field's rule, or null when valid.
  - `static bool isValid(String value, {bool allowWorkspace = false})`
  - `static String problemText(AppLocalizations l10n, ShortNameProblem problem)` — The sentence for a validation problem, for other inputs that check a short name without showing this field.
- **class `ShortNameBox`** — What a [ShortNameField] hands the widget at its right: its text, and the rules its own typing follows.
  - `const ShortNameBox({ required this.controller, required this.formatters, required this.maxLength, required this.onChanged, })`
  - `final TextEditingController controller`
  - `final List<TextInputFormatter> formatters`
  - `final int maxLength`
  - `final ValueChanged<String> onChanged` — What typing calls: the field's own change, which checks the name and reports it.

### `lib/src/split_view.dart`

- **class `SidebarSplitView`** — A resizable sidebar beside a main area, with a thin draggable divider.
  - `const SidebarSplitView({ super.key, required this.sidebar, required this.main, required this.sidebarWidth, this.onSidebarResized, })`
  - `final Widget sidebar`
  - `final Widget main`
  - `final double sidebarWidth`
  - `final ValueChanged<double>? onSidebarResized`
  - `static const double minSidebarWidth`
  - `static const double maxSidebarWidth`

### `lib/src/theme.dart`

- **class `Breakpoints`** — Where the layout changes (§9.1, milestone 4 D11).
  - `static const double treeInDrawer` — Below this **window** width the tree is a drawer and the detail pane has the page to itself.
  - `static const double singleColumn` — Below this **pane** width a form puts one field on a row, whatever width the field asked for.
  - `static const double compareTight` — Below this width a comparison gives away its padding — the page's and the cards' — so that two columns still fit side by side.
  - `static const double compareScrolls` — And below this one there is nothing left to give: the comparison scrolls sideways instead.
  - `static const double labelAbove` — Below this a label sits above its value rather than beside it.
  - `static const double overviewTwoColumns` — Below this the project overview puts its cards in one column.
  - `static const double card` — How wide a card in a grid of them wants to be.
  - `static const double treeHeaderFull` — Below this the tree's header puts the viewer's handle under the project's name rather than beside it.
  - `static const double segmentsCompact` — Below this **card** width Settings' segmented buttons show labels alone, without icons or the selected check.
  - `static const double bodyKeepsHeight` — **Heights**, not widths, and the only two here.
  - `static const double headingsStayPut` — Below this much height a comparison's column headings scroll with its rows instead of standing above them, for the same reason.
  - `static const double headerKeepsHeight` — And what the header keeps for itself on a page too short for both, so that it is cut — with a bar to say so — rather than gone.
- **function** `ThemeData buildTheme(Brightness brightness)` — The application theme: Material 3, compact, one seed colour.
- **class `AppColors`** — Semantic colours the Material scheme does not name.
  - `const AppColors({required this.official, required this.positive, required this.locked})`
  - `final Color official` — The star on the version an official pointer names.
  - `final Color positive` — A positive rating in commentary.
  - `final Color locked` — The ground of a field the reader may not change (§9.3, D6).
  - `static AppColors of(BuildContext context)`
- **variable** `const List<String> kMonoFontFallback` — The monospace stack used for stems, JSON and screenplay text.

## Internal

Files under `lib/` the barrel does not reach; nothing outside the package may import them.

- `lib/src/file_image.dart`
- `lib/src/file_image_io.dart`: `fileImageFor`
- `lib/src/file_image_stub.dart`: `fileImageFor`
