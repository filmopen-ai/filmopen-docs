# filmopen_plugin_dev

*Generated by `scripts/package_digest.py`; regenerate, never edit.*

The plug-in developer page (milestone 5-2 §3.6, the bench plan's D5): a link for a person who writes plug-ins, never for one who only uses them. It reads a plug-in's files again from disk, offers what it exposes in the interface definition's terms — `status` first — and runs a chosen function **as FilmOpen calls it** or **exactly as typed** (milestone 5-2b), the request as sent shown first, — **with none of the question FilmOpen asks before it runs a plug-in a person installed, and every key its manifest names granted** — then shows what the call came to and its log, line by line.

**Flutter.** 2 files, 794 lines under `lib/`; 25 tests in 1 files. Barrel: `lib/filmopen_plugin_dev.dart`.

## Depends on

- **filmopen_format** — uses `Entity`
- **filmopen_state** — uses `FieldRegistry`, `FieldSetter`, `PluginCallControl`, `PluginCallDone`, `PluginCallFailed`, `PluginCallOutcome`, `PluginCatalogueView`, `PluginDevBench`, `PluginDevFunction`, `PluginDevLine`, `PluginDevMode`, `PluginDevPlugin`, `PluginOrigin`, `PluginSummary`, `Settings`
- **filmopen_ui** — uses `KeyValueRow`, `SectionCard`, `kMonoFontFallback`
- **filmopen_l10n** — uses nothing by name the digest could see

## Used by

`filmopen_app`

## Public API

What the barrel exports, file by file. A name is `class`, `enum`, `mixin`, `extension`, `typedef`, a top-level function or variable; under a class, its public constructors, fields, getters, setters and methods.

### `lib/src/plugin_dev_page.dart`

- **function** `Future<void> showPluginDevPage(BuildContext context, {Entity? source, String? stem})` — Opens the plug-in developer page (milestone 5-2 §3.6, the bench plan's D5): a plug-in's own files, run again from disk, with none of the question FilmOpen asks before it runs a plug-in a person installed, and every ke…
- **class `PluginDevPage`** — The plug-in developer page itself (milestone 5-2 §3.6): a plug-in chosen by name, one of its functions, a box of arguments, *Call*, and what the call came to — one plain line, then its log, line by line.
  - `const PluginDevPage({super.key, this.source, this.stem})`
  - `final Entity? source` — Where a render's outputs are saved; null lists them without saving (`PluginDevBench.devRun`'s own `source` parameter).
  - `final String? stem` — The plug-in to open on, where the page was opened from one; null opens with nothing chosen.
